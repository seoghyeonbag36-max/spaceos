# 내일(2026-09-16) 폰으로 할 일 — 붙여 넣는 프롬프트

> 09-08 판 [prompts-mobile-2026-09-08.md](prompts-mobile-2026-09-08.md) 의 다음 판이다.
> 여기 있는 것은 **2026-09-15 에 저장소 파일로 다시 센 자리**뿐이다 — 문서가 아니라 산출물로 셌다.
> 두 경로는 그대로다: **A군 = 폰 앱에 그대로 붙여넣기** · **B군 = claude.ai/code 클라우드 세션 → PR**.
>
> ⚠ **내일의 사정 하나**: 공간정보 공모전 마감이 **2026-09-18**, GovTech 가 **09-21** 이다.
> 원고 두 건에 빈칸이 **8개** 남아 있다. 프론트 정리보다 이게 먼저다.

---

## 0. 먼저 — 09-08 판 중 무엇이 끝났나 (2026-09-15 실측)

| 09-08 번호 | 상태 | 오늘 확인한 근거 |
|---|---|---|
| **B9** 계정 화면 3종 | ❌ **여전히 0** | `grep -rln "로그인\|login\|signup\|회원가입" apps/frontend/src` → **0건**. 백엔드는 이미 다 서 있다 |
| **B16** M2-1 앵커 라벨 | ✅ | `MapShell.tsx:714` 앵커 칩 + `PageDashboard.tsx:225` `<Anchor>` — 둘 다 "모집단이 달라 격차 0 이 목표가 아니다" 문구 동반 |
| **B10** D5-6 글자 정리 | ❌ **역행 심화** | PlatformConsole 989 → **1,236줄** · ProgramStudio 772 → **1,165줄**. 09-13 화면설계서 3판이 +1,538줄을 넣었다(커밋 6777796) |
| **B14** D5-2 하드코딩 색 | 🔶 **전진했으나 미달** | 같은 명령 기준 hex 388 → **342**(목표 100 이하) · `var(--)` 543 → **1,072** → 채택률 **75.8%**(목표 80%) |
| **A5** 도메인 | ✅ **끝났다 — 목록에서 뺀다** | `placeos.web.app` 이 09-13 정식 주소로 확정됐고, 옛 주소 처리까지 코드로 섰다(`src/lib/canonicalHost.ts` + 테스트). 남은 `spaceos-twin` 표기 15곳은 **리다이렉트 기계·키 등록 메모·과거 기록**이라 고치면 오히려 틀려진다 |
| **A9** D4 랜딩 | ⬜ 미착수 | `apps/landing/` 없음. PRD(`docs/prd-landing.md`)만 있다 |
| B5 컴포넌트 실투입 | 🔶 | `@/design/components` import **파일 4개** — 09-13 에 새로 들어온 컴포넌트 3종이 쓰지 않았다 |
| A7 아웃리치 · A10 데모 대본 | ⬜ 저장소에 산출물 없음 | 폰 작업이라 저장소 밖일 수 있다 — **판정 보류** |

**그리고 09-08 판에 없던 것 두 개가 오늘 드러났다.**

1. **공모전 원고 빈칸 8개** — `python scripts/check_application.py` 는 **위반 0건**으로 통과하지만
   그 옆에 `FILL 잔여 5 / 3` 이 찍힌다. 위반이 없는 건 **아직 안 썼기 때문**이다.
   마감은 **09-18(공간정보) · 09-21(GovTech)**.
2. **계정층 백엔드는 이미 5개가 돈다** — `/api/v1/auth/{signup,login,me,api-keys×2}`
   (`apps/backend/app/api/v1/auth.py` · `models/auth.py` · alembic). 프론트에만 **사람이 가입할 화면이 없다.**
   `pppp_status.py` 밑에 적힌 *"배선 100% 는 파일럿 0 건과 양립한다"* 의 절반이 이 한 화면이다.

---

## 1. 내일의 우선순위 한 줄

**A1(공간정보 원고) → A2(GovTech 원고) → B1(계정 화면) → A3(계정 문구)** 순서다.
A1 은 **모레가 마감**이라 내일이 사실상 마지막 작성일이다. B2·B4 는 남는 시간에만.
09-15 에 막힌 **GNN 재학습·소분류 어휘는 폰으로 안 된다**(§3).

---

# A군 — 폰만으로 완결 (Claude·GPT 모바일 앱에 그대로 붙여넣기)

> A1·A2 는 저장소를 못 보는 폰에서도 쓸 수 있게 **필요한 수치를 프롬프트 안에 다 넣었다.**
> 산출물은 메모에 받아 두었다가 데스크톱에서 `draft.md` 의 해당 `<!-- FILL -->` 자리에 붙이고
> `python scripts/check_application.py --doc <원고> --require-complete` 로 통과시킨다.

## A1 · 공간정보 공모전 원고 빈칸 5개 ★마감 09-18 · 가장 먼저 (60분)

```
너는 PlaceOS 창업자의 공모전 원고 작성자다. 제8회 공간정보 활용·아이디어 경진대회
(국토교통부·공간정보산업진흥원 · 부문: 공간정보 활용 사례) 출품 원고의 빈 절 5개를 쓴다.

[서비스 한 줄]
PlaceOS — 상권 단위로 뭉쳐 있던 공실을 **건물·층 단위**로 내려 읽는 서비스.
아이디어가 아니라 접속 가능한 프로덕션이다(https://placeos.web.app).

[쓸 수 있는 수치 — 이 목록 밖의 숫자를 절대 만들지 마라]
 · 66거점 — 서울. **'건축물대장' 이라는 한정어를 같은 절에 반드시 함께 쓴다.**
   한정 없이 쓰면 실측 정확도 주장으로 읽힌다
 · 52,642 — 마스터 폴리곤 수. **'동결한 입력 묶음 기준'임을 밝힌다**
 · 664유닛 — 서빙 66거점의 실제 공실 유닛 인벤토리
 · GNN 업종추천 Top-1 64.9% / Top-3 91.7%
 · lift 5.3% — **반드시 기준선을 함께 밝힌다**(거점 사전분포 Top-1 61.7% · Top-3 89.4%)
 · LSTM 공실 예측 **방향 정확도** 70.8% — **목표 70%를 0.8%p 차로 넘는다는 점을 함께 적는다**
 데이터 출처(이미 원고 3절에 표로 있다): 브이월드 GIS건물통합정보 · 건축HUB 건축물대장 ·
 소상공인 상가정보 · LOCALDATA 인허가 · R-ONE 임대동향 · 서울 열린데이터광장 · 네이버 지도

[절대 금지 — 하나라도 쓰면 그 원고는 실격 취급이다]
 ✗ "실측 공실률" 이라는 표현 · 현실 공실 정확도 주장 (우리가 만든 건 산출물이지 정답이 아니다)
 ✗ "공실 예측 정확도" (측정한 것은 **방향** 정확도다. 오르는가 내리는가)
 ✗ 3D · 3D 디지털 트윈 · Three.js · Mapbox · Vercel — 전부 제거·폐기됐다.
   대면심사에서 시연을 요구받으면 그 자리에서 깨진다. 현재는 **2D 층 스택 + 네이버 거리뷰**다
 ✗ 파일럿·고객사·도입사 실적 (계측 배선은 4/4로 섰으나 실적은 미확인이다. **목표로만** 쓴다)
 ✗ 보류 거점을 서빙 거점 수에 합산하기 (고양·파주는 서빙 보류다)
 ✗ 라이선스·이용조건 단정 (확인 안 됐다. 필요하면 "[확인 필요]" 로 남긴다)

[쓸 절 5개 — 각각 분량과 핵심 문장이 정해져 있다]

1절 「개요」 300~400자
  핵심: 상권 단위로 뭉쳐 있던 공실을 건물·층 단위로 내려 읽는다. 아이디어가 아니라 프로덕션이다.

2절 「무엇을 푸는가」 500~700자 (배점: 구체성 10 · 타당성 10)
  핵심: 공공 통계는 상권 단위까지만 답한다. "어느 건물 몇 층이 비었나"에 답하는 자료가
        없어서 입점·정책 판단이 늘 한 단계 위에서 멈춘다.
  쓸 것: 마스터 폴리곤 52,642(동결 입력 묶음 기준) · 공실 유닛 664.

4절 「구현 결과」 600~800자 (배점: 실현성 20 · 완결성 10)
  구조: (1) 접속 가능한 프로덕션 URL (2) 화면 4종 — 공실 히트맵 / 2D 층 스택 /
        네이버 거리뷰 / 업종 추천 (3) 모델 성능 (4) 재현성(수집기와 실행 명령이 저장소에 있다).

5절 「누가 쓰는가」 300~400자 (배점: 활용성 10)
  수요자 셋에 한 줄씩 — 프랜차이즈 본사(출점 후보 선별) · 자산운용사(보유 자산 공실 모니터링)
  · 지자체(빈 상가 대응). 각각 **화면에서 무엇을 얻는지**로 쓴다.

6절 「이어지는가」 300~400자 (배점: 지속성 10)
  핵심: 거점 추가가 문서화된 선형 체인으로 서 있다 — 후보 판정 → 등록 → 수집 → Gold 빌드
        → 앵커 대조 → 서빙 등재 → 검증. 즉 확장 비용이 예측 가능하다.
  정직하게: 서울 밖(경기) 확장 절차도 규정돼 있으나 **현재 서빙 보류**라고 밝힌다.

[문체]
 한국어. 기술 용어는 영문 병기. 심사위원이 배점표를 들고 읽는다 — 절마다 배점 항목에
 대응하는 문장이 첫 두 줄 안에 나와야 한다. 수식어보다 숫자와 경로를 쓴다.

[낼 것] 5개 절의 완성 원고. 절마다 글자 수를 괄호로 적는다.
그리고 맨 끝에 "이 원고에서 쓴 수치와 그 조건" 표를 붙인다(수치 · 조건 · 쓴 절).
```

## A2 · GovTech 원고 빈칸 3개 — 마감 09-21 (45분)

```
2026 GovTech 창업 경진대회(과학기술정보통신부·디지털플랫폼정부위원회 ·
트랙: 제품·서비스 개발 × 지역·사회 문제 해결) 사업계획서 원고의 빈 절 3개를 쓴다.

[이 대회의 결정적 제약 — 먼저 읽어라]
 **M&A Exit 서사를 쓰지 않는다.** 심사축이 공공서비스 혁신과 공공성이라
 포털 대상 매각 서사는 감점으로 읽힌다. 같은 사실은
 "공공·민간 플랫폼과의 데이터 연계 확장"으로 쓴다.

[서비스] PlaceOS — 상권 공실을 건물·층 단위로 내려 읽고, 그 자리에 무엇을 얼마에 열고
어떻게 알릴지까지 잇는 상권 디지털 트윈. 프로덕션 https://placeos.web.app

[쓸 수 있는 수치 — 이 밖의 숫자를 만들지 마라]
 66거점(건축물대장 기반 · 서울) · 공실 유닛 664 · 업종추천 Top-3 91.7%
 공실 예측 **방향** 정확도 70.8% · DaaS 월 500만원 B2B 구독 · 6개월차 파일럿 5~10건 **목표**

[쓸 절 3개]

1절 「문제인식」 600~800자
  핵심: 상권 공실은 행정이 **사후에만** 아는 문제다. 빈 점포가 생겼다는 사실은 알아도
        어느 건물 몇 층이 왜 비었고 무엇으로 채워야 하는지는 어떤 공공 데이터도 답하지 않는다.
        지자체의 공실 대응이 늘 늦는 구조적 이유다.
  구조: (1) 누가 곤란한가 — 지자체 상권 담당 (2) 지금 어떻게 하고 있나 — 상권 단위 통계와
        현장 실사 (3) 왜 그것으로 부족한가.

3절 「성장전략」 600~800자
  구조: (1) 수익 모델 — DaaS 월 구독 B2B (2) 고객군 — 프랜차이즈 본사·자산운용사·지자체
        (3) 확장 경로 — 거점 온보딩이 문서화된 선형 체인이라 도시 추가 비용이 예측 가능하다
        (4) 공공 조달 경로.
  ⚠ 파일럿 건수는 **목표로만**. "확보·달성·운영 중" 같은 표현을 쓰지 마라 — 실적은 미확인이다.

4절 「팀」 400~500자
  각도: **1인이라는 사실을 감추지 않는다.** 대신 검증 규율 자체를 역량으로 제시한다 —
        기각한 가설을 기록으로 남기고, 진행률을 문서가 아니라 산출물에서 세는 스크립트로 관리한다.
        1인 조직이 이 정도 재현성을 유지한다는 것이 곧 실행력의 증거다.
  ⚠ 없는 팀원·자문단을 만들지 마라. 외부 협력이 없으면 없다고 쓴다.

[절대 금지] 3D·Three.js·Mapbox·Vercel · "공실 예측 정확도"(방향 정확도다) ·
파일럿 실적 주장 · M&A/매각 서사 · 출처 없는 시장 규모 숫자

[낼 것] 3개 절의 완성 원고 + 절마다 글자 수.
```

## A3 · 계정 화면 3종 문구·플로우 확정 — 20분 ★B1 의 선행

> **왜 지금**: 백엔드 오류 문구는 이미 한국어로 고정돼 있다. 화면 문구를 폰에서 먼저
> 정해 두면 B1 클라우드 세션이 **문구를 지어내지 않는다.**

```
너는 PlaceOS 의 UX 라이터다. B2B 파일럿 온보딩 화면 3종의 문구를 확정한다.

[제품] PlaceOS — 서울 66거점의 건물·층 공실을 건축물대장 기반으로 읽고, 입점 회수기간
(3-Tier)과 홍보 program 까지 잇는 상권 디지털 트윈 SaaS.
[대상] 프랜차이즈 본사 출점팀 · 자산운용사 리서치 · 자치구 상권활성화 담당,
       그리고 창업자·업종을 바꾸려는 사업자(09-13 주요 고객 재정의).
[모델] DaaS 월 500만원 B2B 구독. **조직(org) 단위로 가입**하고 조직 관리자가 API 키를 발급한다.

[이미 고정된 것 — 바꾸지 말고 화면 문구를 여기에 맞춰라]
 · 가입은 개인이 아니라 조직 생성이다. 필드는 조직명 / 이메일 / 비밀번호 셋뿐
 · 서버 오류 문구 3개가 확정돼 있다. 그대로 쓴다:
     409 "이미 가입된 이메일입니다"
     401 "이메일 또는 비밀번호가 올바르지 않습니다"
     403 "이 작업은 조직 관리자만 할 수 있습니다"
 · API 키 평문은 **발급 직후 한 번만** 보인다(서버가 평문을 저장하지 않는다)
 · 분석 화면(지도·히트맵·추천)은 로그인 없이도 그대로 공개다 — 로그인은 기존 서빙을
   인증 뒤로 숨기는 게 아니라 파일럿 온보딩용이다. 화면에서 이걸 오해시키지 마라

[낼 것 — 표로]
 1. 화면 3종의 제목·부제·버튼 문구  ① 로그인 ② 조직 가입 ③ 계정/API 키
 2. 각 화면의 빈 상태·로딩·에러 문구 (서버 문구 3개는 그대로)
 3. API 키 화면의 경고 한 줄 — "다시 볼 수 없다"를 겁주지 않고 말한다
 4. 가입 전에 사람이 궁금한 것 3개와 그 자리에 놓을 한 줄 답
    (무료인가 / 우리 데이터를 넣어야 하나 / 지도는 로그인해야 보이나)
 5. 로그인 화면에서 분석 화면으로 **돌아가는 길** 문구 — 막다른 골목을 만들지 마라

[금지]
 - 실측 밖 숫자(필요하면 "[측정 필요]") - "AI 기반"·"올인원" 같은 빈 수식어
 - 비밀번호 규칙 창작("[서버 확인]" 으로 남긴다) - 소셜 로그인·SSO 전제(지금 없다)
```

## A4 · B2B 파일럿 접촉 실행 — 퇴근길 30분 ★KPI② 나머지 절반

> 09-08 의 A7 은 *접촉안* 이었다. 내일은 **명단과 첫 3통**이다.
> 계측은 이미 돈다 — `GET /api/v1/admin/usage` 의 `active_orgs` 가 그대로 파일럿 수다.

```
PlaceOS B2B 파일럿 접촉을 오늘 실제로 보낼 수 있는 형태까지 내린다.
접촉안을 다시 쓰지 말고 명단과 발송본을 만든다.

[쓸 수 있는 실측 — 이 밖의 숫자를 만들지 마라]
 서울 66거점(건축물대장 기반) · 공실 유닛 664 · 업종추천 Top-3 91.7%
 (거점 사전분포 기준선 89.4% 대비 Top-1 lift 5.3%) · 공실 예측 방향 정확도 70.8%
 히트맵 4레이어(공실·임대·유동·밀도)가 같은 100m 격자 · 프로덕션 https://placeos.web.app

[낼 것]
 1. 접촉 후보 20곳 — 군별로(프랜차이즈 본사 / 자산운용·리츠 / 자치구).
    각 줄: 조직명 + 역할(직함) + 그 조직이 지금 이 일을 무엇으로 하고 있는지 추정.
    ⚠ 개인 이름·이메일·전화번호를 지어내지 마라. 확인 안 된 칸은 "[확인 필요]"
 2. 상위 3곳을 고른 이유 한 줄씩
 3. 그 3곳에 바로 보낼 메일 본문 3통 — 각 120자 이내 + 제목 2안.
    첫 문장에 그 조직의 구체적 상황이 들어가야 한다. 템플릿 냄새가 나면 실패다
 4. 회신 오면 15분 안에 보여줄 화면 순서 3단계 (지도 → 건물 층 스택 → 3-Tier 회수기간)
 5. 파일럿 제안 1장 — 기간 / 그들이 내는 것 / 우리가 받는 것(피드백·사용로그)

[반드시 먼저 말할 것 — 앵커 격차]
 우리 전수 공실률은 R-ONE 표본 공실률보다 높고, 중대형만 남기면 격차는 더 커진다
 (연남 20.8% vs 5.0%). 원인은 큰 건물의 분자 결손이다.
 "우리가 전수라서 높다"는 설명은 실측이 반대를 가리키므로 쓰면 안 된다.

[금지] 연락처·담당자 실명 창작 · 실측 밖 숫자 · 가격 협상 약속
```

## A5 · D4 랜딩 생성 (Lovable·v0) — 저녁 1시간 · 남는 시간에만

```
아래로 한 페이지 랜딩을 만든다. 파일 하나(HTML+CSS, 외부 JS 프레임워크 없이)로 낸다.

[제품 한 줄] PlaceOS — 어느 건물 몇 층이 비었는지를 건축물대장 기반으로 읽고,
그 자리에 무엇을 얼마에 열고 어떻게 알릴지까지 잇는 상권 디지털 트윈.

[섹션 순서 — 이대로]
 1. 히어로: 한 문장 + 실측 한 줄(서울 66거점 · 664유닛) + CTA "파일럿 문의"
 2. 문제: 지금은 발품과 표본 통계뿐이다
 3. PPPP 네 트랙 한 줄씩 — Platform(어떤 상권인가) / Page(어디가 비었나) /
    Posting(얼마에 들어가나) / Program(어떻게 알리나)
 4. 근거: 어디서 온 숫자인가 (건축물대장 · 상가정보 · R-ONE · 집계구 생활인구)
 5. 한계 고지 1문단 — 앵커 격차와 거리뷰 촬영 시점을 **먼저** 말한다
 6. CTA: 파일럿 문의 폼(이름·조직·역할·이메일)

[디자인 제약]
 · 색은 :root 의 CSS 변수로만 선언한다. 본문에 hex 를 직접 쓰지 마라 — 저장소 규칙이다
 · 4트랙 색 축 변수를 만들고 섹션 3에서만 쓴다
 · 모바일 400px 에서 가로 스크롤이 생기면 안 된다
 · 한글 본문. 기술 용어는 영문 병기

[금지] 스톡 사진·가짜 로고·가짜 고객사·가짜 후기 · 실측 밖 숫자 ·
"업계 1위"·"AI 혁신" · 폼 action 을 실제 주소로 채우기(비워 두고 주석)
```

---

# B군 — 클라우드 세션 지시 (claude.ai/code → PR)

> 폰에서 **claude.ai/code** 를 열고 그대로 붙여넣는다. 저장소 규칙대로
> **대상 파일 / 통과 조건 / 금지** 를 채워 뒀다. 한 세션에 하나씩 걸 것.

## B1 · 계정 화면 3종 ★KPI② 직결

```
apps/frontend 에 계정 화면 3종을 붙여줘. 백엔드는 이미 다 서 있다 — 새로 만들지 마라.

[이미 있는 것 — 먼저 읽고 필드명을 맞춰라]
 apps/backend/app/api/v1/auth.py 에 5개가 돈다:
   POST /api/v1/auth/signup   {org_name, email, password} → {access_token}  (409 이미 가입)
   POST /api/v1/auth/login    {email, password}           → {access_token}  (401 불일치)
   GET  /api/v1/auth/me       → {user_id, email, org{id,name}, role}
   POST /api/v1/auth/api-keys → 발급(관리자만 · 403). 평문은 이 응답에만 있다
   GET  /api/v1/auth/api-keys → 목록(평문 없음)
 스키마: apps/backend/app/schemas/auth.py

[대상 파일]
 apps/frontend/src/lib/api.ts          — 호출 5개 추가(이 파일로 일원화. fetch 직접 호출 금지)
 apps/frontend/src/lib/session.ts      — 신규. 토큰 보관·만료·로그아웃
 apps/frontend/src/pages/AccountShell.tsx(+ .css) — 신규. 로그인·가입·계정 3화면
 apps/frontend/src/App.tsx             — 진입점 연결(레일의 PPPP 네 트랙은 건드리지 마라)
 apps/frontend/src/pages/AccountShell.test.tsx — 신규

[문구] docs/prompts-mobile-2026-09-16.md §A3 로 확정한 문구가 있으면 그것을 쓴다.
 없으면 백엔드 오류 문구 3개를 그대로 띄우고 나머지는 최소한으로 적는다.

[지켜야 할 제약]
 · 분석 화면(지도·히트맵·추천)은 로그인 없이 그대로 보여야 한다. 라우팅을 인증 뒤로 옮기지 마라
 · API 키 평문은 발급 응답에만 있다 — **localStorage 에 저장하지 마라.** 화면에 한 번 보이고 끝
 · 토큰은 한 곳(session.ts)에서만 읽고 쓴다
 · 색은 var(--…) 만 쓴다. hex 를 새로 쓰지 마라 — 지금 342개를 줄이는 중이다
 · 새 컴포넌트는 @/design/components 를 먼저 찾아 쓰고 없을 때만 만든다

[통과 조건]
 cd apps/frontend && npm run build     # tsc -b 통과
 cd apps/frontend && npm test          # vitest. 기존 테스트 파일 11개 전부 통과 + 아래 4개 신규
   · 가입 성공 → /auth/me 의 조직명이 화면에 뜬다
   · 409 → "이미 가입된 이메일입니다" 가 화면에 뜬다
   · 401 → "이메일 또는 비밀번호가 올바르지 않습니다" 가 화면에 뜬다
   · 로그아웃 후에도 지도 화면이 열린다(공개 경로 불변)

[금지]
 - 백엔드 수정 · 새 엔드포인트 · 새 의존성 설치
 - 소셜 로그인·SSO·비밀번호 재설정 (엔드포인트가 없다)
 - 더미 계정 하드코딩(필요하면 테스트 안에서만)
 - 지도 생명주기(MapHost) 건드리기
```

## B2 · D5-2 잔여 — 하드코딩 색 342 → 150 이하

```
apps/frontend 의 하드코딩 색을 토큰으로 바꿔줘. **값은 바꾸지 말고 이름만 붙인다.**

[현재 실측 2026-09-15]
 hex 342 · var(--) 1,072 → 채택률 75.8% (목표 80% · hex 목표 100 이하)
 명령: cd apps/frontend && grep -oE "#[0-9a-fA-F]{3,8}" src/pages/*.css src/components/*.css src/App.css | wc -l
 상위 4파일이 281개다:
   PlatformConsole.css 87 · PageDashboard.css 79 · ProgramStudio.css 64 · PostingConsole.css 51

[대상 파일] 위 4개 css + src/styles/tokens.css (토큰 추가는 여기에만)

[방법]
 1. 4파일의 hex 를 모아 같은 값끼리 묶고 묶음마다 역할 이름을 붙인다
    (표면/테두리/본문/보조텍스트/성공/경고/위험/트랙색 …)
 2. tokens.css 에 같은 값이 이미 있으면 새로 만들지 말고 그것을 쓴다
 3. 역할이 불분명한 1회성 색은 그대로 둔다 — 억지 토큰은 다음 판의 부채다
 4. 무엇을 남겼는지 PR 본문에 파일별 잔여 개수로 적는다

[통과 조건]
 cd apps/frontend && npm run build && npm test     # 기존 테스트 파일 11개 전부 통과
 hex 342 → 150 이하. PR 본문에 같은 명령으로 찍은 전후 숫자
 ⚠ 렌더 결과가 바뀌면 안 된다 — 대체가 아니라 이름 붙이기다

[금지]
 - tailwind 재도입(설치된 적 없고 설정도 09-06 에 지웠다)
 - 색 값 변경 · 다크모드 신설 · 컴포넌트 구조 변경
 - .tsx 인라인 style 까지 같이 손대기(범위를 넓히지 마라)
```

## B3 · D5-6 글자 정리 — 남는 시간에만

```
PlatformConsole.tsx(1,236줄) · ProgramStudio.tsx(1,165줄) 를 줄여줘.
09-13 화면설계서 3판이 +1,538줄을 넣으면서 두 파일이 다시 커졌다.

[방법 — 이 순서로만]
 1. 파일 안에서 한 번만 쓰이는 하위 컴포넌트를 같은 폴더의 별도 파일로 뺀다
 2. 순수 계산(포맷·정렬·집계)을 src/lib/ 로 뺀다
 3. 그래도 남으면 거기서 멈춘다 — 동작을 바꿔서 줄이지 마라

[통과 조건]
 cd apps/frontend && npm run build && npm test   # 기존 테스트 파일 11개 전부 통과. 테스트를 고치지 마라
 두 파일 각각 900줄 이하. PR 본문에 전후 wc -l

[금지] 동작·문구·DOM 구조 변경 · 테스트 수정 · 새 상태관리 라이브러리 ·
지도 생명주기(MapHost) 건드리기
```

---

## 2. 폰으로는 못 하는 것 — 데스크톱 대기 (09-15 에 실제로 막힌 것)

| 작업 | 왜 폰·클라우드에서 안 되나 |
|---|---|
| **소분류 어휘 실측** ([prompt-store-taxonomy-scls-2026-09-15.md](prompt-store-taxonomy-scls-2026-09-15.md)) | Bronze(`stores_raw.json`)가 클라우드에 없다(.gitignore). `DATA_GO_KR_SERVICE_KEY` 도 없고 네트워크 정책이 data.go.kr 을 막는다(CONNECT 403). **`편의점`·`약국`·`커피전문점` 세 규칙이 아직 미검증 추측이다** |
| **GNN 재학습** ([prompt-gnn-retrain-scls-2026-09-15.md](prompt-gnn-retrain-scls-2026-09-15.md)) | 위가 선행이다. 라벨이 틀린 채 학습하면 틀린 모델이 나온다. 대조군을 만들 수 없으므로 **새 기준선 수립**으로 적어야 한다 |
| 수집(건축HUB·R-ONE·네이버) · LSTM 재학습 | 원천과 키가 로컬에만 있다 |
| 지도 픽셀 검증(`/verify`) · 66거점 화면 루프 | 네이버 지도 SDK 키 + 로컬 서버 + playwright 실행 |
| 공모전 원고 **제출** | 서식 4종 다운로드·동의서 스캔·국세청 사실증명이 필요하다. 원고 작성까지가 폰 몫이다 |
| Lovable 산출물을 `apps/landing/` 으로 편입 | 클론·빌드·배포 라인 작업 |

**데스크톱에 앉으면 이 순서로**:

```bash
# 1) 폰에서 쓴 원고를 FILL 자리에 붙인 뒤 — 이게 마감 게이트다
python scripts/check_application.py --doc docs/apply/01-spatial-info/draft.md --require-complete
python scripts/check_application.py --doc docs/apply/02-govtech/draft.md --require-complete
# 2) 그다음 데이터 쪽. 소분류 어휘 → GNN 재학습 순서(위 표의 두 문서)
python scripts/pppp_status.py | grep -v '└'
```

---

## 3. 내일 저녁에 다시 셀 명령 (다음 판을 위해)

```bash
python scripts/check_application.py                                    # FILL 잔여 5·3 기준
cd apps/frontend && grep -rln "로그인\|login\|signup\|회원가입" src | wc -l   # B1 — 0 이면 미착수
cd apps/frontend && grep -oE "#[0-9a-fA-F]{3,8}" src/pages/*.css src/components/*.css src/App.css | wc -l  # B2 — 342 기준
cd apps/frontend && wc -l src/pages/*.tsx | sort -rn | head -5          # B3 — 1236·1165 기준
curl -H "X-Admin-Token: $ADMIN_TOKEN" https://placeos.web.app/api/v1/admin/usage   # KPI② active_orgs
```
