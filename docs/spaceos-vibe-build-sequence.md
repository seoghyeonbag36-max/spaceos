# SpaceOS 바이브 코딩 빌드 순서 (Page 중심 → 전체 PPPP)

> 목적: "SpaceOS를 만들기 위해 바이브 코딩으로 수행할 단계"를 의존성 순서로 정리. 지금까지 한 작업(거점 확정·데이터 조사·건물공실 PoC 설계·D1 스키마)이 어디에 위치하는지, 앞서 미뤄둔 (A)·(B)가 언제 들어오는지 명시한다.

## 원칙
- **1단계 = 1 바이브 사이클**: PRD(자연어) → AI 코드 생성(Cursor/Claude Code + 슬래시 명령) → `/verify` 검증.
- **데이터 흐름**: Bronze(수집) → Silver(정제·매칭) → Gold(분석·GeoJSON). `/api/v1/...` 규약.
- **데이터가 UI를 결정(회의 3번)**: 먼저 데이터를 확보하고, 지도 위 표현 방식을 그 데이터로 결정.
- **회의 1번(네이버 지도 확대)**은 데이터와 독립 → 언제든 병렬 착수 가능.

---

## Phase 0 — 결정·기반 (대부분 완료)
| # | 단계 | 상태 | 산출물 |
|---|---|---|---|
| 0-1 | PoC 거점 확정 = 강남구 신사동 **가로수길** | ✅ | [poc-building-vacancy.md §0.5](poc-building-vacancy.md) |
| 0-2 | 4대 데이터 소스 조사 + 건물공실 PoC 설계 + D1 스키마 확정 | ✅ | poc-building-vacancy.md, [probe_garosu_d1.py](../data/probe_garosu_d1.py) |
| 0-3 | **(A) 인증키 5종 발급 + 응답 필드 스펙 확정** | ⬅ **다음 필수** | `.env` 채움 + 필드 매핑표 |

**(A) 발급 대상 5종:**
1. 공공데이터포털 `DATA_GO_KR_SERVICE_KEY` — ① 소상공인 상가정보(15012005) ② 건축물대장(15134735 건축HUB — 구 15044713 서비스 종료) *두 API 한 키로 공용*
2. 서울 열린데이터광장 `SEOUL_OPENAPI_KEY` — 실시간 도시데이터/생활인구(유동인구)
3. 통계청 SGIS 키 — 인구밀도(총조사 격자)
4. 한국부동산원 R-ONE / data.go.kr 키 — 공실률·임대시세 (①의 data.go.kr 키로 공용 가능)
5. 네이버 개발자센터 `NAVER_CLIENT_ID/SECRET` — 지도 + 검색/데이터랩
> → GIS건물통합정보(15083092)는 API가 아니라 **강남구 폴리곤 파일 다운로드**.

---

## Phase 1 — 데이터 수집 (Bronze) · 쉬운→어려운 순, 상호 독립이라 병렬 가능
| # | 데이터 | 소스 | 명령 | 난이도 |
|---|---|---|---|---|
| 1-1 | **임대시세 + 거시 공실률** | 한국부동산원 API | `/backend-dev` 콜렉터 | 하 |
| 1-2 | **인구밀도** | 통계청 SGIS API | 콜렉터 | 하 |
| 1-3 | **유동인구** | 서울 실시간 도시데이터 API | `living_population.py` 확장 | 중 |
| 1-4 | **건물 단위 공실** | 상가정보×건축물대장×GIS폴리곤 | `probe_garosu_d1` 관통 → `building_vacancy.py` | **상(핵심)** |

---

## Phase 2 — 가공 (Silver/Gold + PostGIS)
| # | 단계 | 산출물 |
|---|---|---|
| 2-1 | PostGIS 스키마 생성 (`dim_building`, `fact_store`, `building_vacancy`) | DB 마이그레이션 |
| 2-2 | 점포↔건물 매칭 (`bdMgtSn` 그룹핑 + 좌표 PIP) → occupancy 산출 | `map_store_building` |
| 2-3 | 4대 데이터 → Gold GeoJSON (구 단위 히트맵 + 건물 포인트) | `*.geojson` |

---

## Phase 3 — 백엔드 API (더미 → 실데이터)
| # | 단계 | 상태 | 대상 파일 | 명령 |
|---|---|---|---|---|
| 3-1 | `/api/v1/heatmap` GeoJSON (Gold 기반) | ✅ 2026-08-01 | `services/gold_vacancy.py` 신설 · `districts.py` 배선 교체 | `/backend-dev` |
| 3-2 | `/api/v1/buildings/{id}` 공실·히스토리 실데이터 | ⬜ | `buildings.py` 더미 교체 | `/backend-dev` |
| 3-3 | 4대 데이터 레이어 토글 응답(유동/공실/임대/밀도) | ⬜ | `heatmap.py` | `/backend-dev` |

### 3-1 결과 — 시드/합성 → Gold 실측 배선 (2026-08-01)
`services/districts.build_cells()` 의 `sin()` 해시 합성이 거점 공실의 유일한 출처였다.
이제 Gold 건물 마스터가 있는 거점은 실측 집계를 쓰고, 없는 거점만 합성으로 폴백한다.

- **적용 범위**: 54거점 중 Gold 보유 **13거점**이 실측. 나머지 41거점은 합성 유지 —
  `data/pipelines/build_page_master.py` 로 Gold 를 만들면 코드 수정 없이 자동 전환된다.
- **집계 규칙 3종** (`services/gold_vacancy.py`, `calibration.json` 과 동일 기준):
  | # | 배제 대상 | 이유 |
  |---|---|---|
  | 1 | `capacity_method == floor_approx` | 지상 **전체** 층수 근사 → 주거·사무층까지 상가로 세어 분모가 부푼다 (가로수길 전수 33.1% vs 24.2%) |
  | 2 | `capacity_method == expos_units` (집합건물) | 상가정보가 집합상가 **내부** 점포를 그 건물 bdMgtSn 으로 귀속시키지 못해 분자가 빈다 → 공실률 78~86% |
  | 3 | `source == polygon_only` | 점포 미매칭이라 `active=0`, 공실률 100% 고정 |
  세 규칙은 서로 독립이라 각각 검사한다(재빌드가 조합을 바꿀 수 있다).
- **출처 표기 필수**: 응답의 `vacancy_source`("gold"/"synthetic")를 프론트가 배지로 노출한다.
  합성값이 실측처럼 읽히면 안 된다.
- **격자**: 셀 크기·원점은 시드 grid 를 그대로 쓰되 인덱스가 bb 밖으로 나갈 수 있게 두어,
  수집 반경이 시드 bb 를 벗어난 건물을 버리지 않는다(가로수길 72셀 중 일부가 bb 밖).

### 3-1 앵커 대조 (2026-08-01) — 집합건물 혼입이 대표값을 무너뜨리고 있었다
초판 배선은 규칙 1·3만 적용해 집합건물을 섞었고, 5거점이 50~67% 로 튀었다.
`python -m data.pipelines.calibrate_vacancy <거점>` 으로 앵커 대조한 결과 원인이 확정됐다:
**집합건물은 건물 수로는 소수인데 호실이 많아 분모의 52~82% 를 차지**한다.

| 거점 | 혼합(초판) | 일반만(현재) | R-ONE 앵커 | 격차 | 집합이 분모에서 |
|---|---|---|---|---|---|
| seoulsup | 67.0% | **19.8%** | 3.4% | +16.4%p | 82% |
| ikseon | 57.1% | **30.1%** | 8.5% | +21.5%p | 52% |
| myeongdong | 56.3% | **16.9%** | 5.0% | +11.9%p | 59% |
| yeonnam | 52.3% | **16.6%** | 4.5% | +12.1%p | 52% |
| euljiro | 51.3% | **17.7%** | 6.8% | +10.9%p | 53% |

13거점 전체 범위: **13.3~30.1%**, 앵커 격차 **-4.0 ~ +21.5%p**
(itaewon +0.4 · hannam +2.0 · garosugil +3.2 가 가장 정합, songridan 은 -4.0 으로 과소).
격차가 0 이 되는 것이 정상은 아니다 — 우리는 호실·전수, R-ONE 은 면적·표본이라
모집단이 다르다. 절대값이 아니라 **거점 간 비교·추세 감시**에 쓴다.
API 는 `anchor_pct` / `anchor_gap_pp` 로 이 대조를 함께 내려보낸다.

⚠ 집합건물 공실률 자체는 여전히 신뢰 구간 밖이다. 층·호 단위 매칭(flrNo/hoNo)을
붙이기 전까지 대표 집계에서 뺀 채로 둔다(건물 폴리곤 레이어에는 계속 그린다).
남은 최대 격차는 ikseon +21.5%p — 익선동은 한옥 소형 호실이 많아 호실 기준이
면적 기준보다 높게 나오는 것으로 보이나, 확인은 미실시.

---

## Phase 4 — 프론트 Page (회의 1·3번)
| # | 단계 | 상태 | 명령 | 비고 |
|---|---|---|---|---|
| 4-1 | **(B) 네이버 지도 영역 확대·레이아웃** | ✅ 2026-08-01 | `/page`, `/frontend-dev` | 회의 1번. MapShell 을 App 네비 "지도" 탭에 연결 |
| 4-2 | 4대 데이터 히트맵 레이어 + 토글 | 🔶 공실만 실데이터 | `/page` | 유동인구는 샘플, 임대·밀도는 미연동 |
| 4-3 | 건물 클릭 → 공실 상세 / 3D 디지털 트윈 | ✅ | `/page` | `BuildingTwin` |
| 4-4 | 시간 슬라이더 = 유동인구 흐름 애니메이션 | 🔶 UI만 | `/page` | 회의 ※중요포인트 "흐름 시각화" — 슬라이더는 있으나 데이터가 샘플 |

### 4-1 결과 — MapShell 라우팅 복구 (2026-08-01)
289줄짜리 지도-우선 풀스크린 레이아웃이 만들어져 있었는데 `App.tsx` 어디에서도 렌더되지
않아 방치돼 있었다. 네비에 **"지도"** 탭을 붙여 연결했다.

- **거점 하드코딩 제거**: `gangnam-garosugil` 고정 → 실측 거점 목록에서 선택.
  `vacancy_source === "gold"` 인 거점만 담는다(합성 거점은 `/heatmap/buildings` 가 404).
  3-1 이 붙인 필드를 그대로 재사용하므로 Gold 를 새로 만들면 선택지가 자동으로 늘어난다.
- **레이아웃**: MapShell 이 `position:absolute·inset:0` 이라 위치 지정된 부모가 필요하다.
  지도 뷰에서만 루트를 flex 컬럼(`height:100dvh`)으로 두고 `main` 을 `position:relative`
  로 만든다. 대시보드 두 뷰는 기존대로 문서 스크롤 하나만 쓴다.
- **지도가 안 보이던 진짜 이유**: 네이버 지도 SDK 가 초기화하면서 컨테이너의 `position` 을
  `relative` 로 덮어써 `inset:0` 이 사이징으로 동작하지 않고 높이가 0 으로 접혔다.
  `.map-canvas` 에 `width/height:100%` 를 같이 준다.
- 코드 분할: 지도 탭은 lazy import 라 대시보드 사용자는 네이버 SDK·3D 번들을 받지 않는다.

---

## Phase 5 — ML (Platform 지능화)
| # | 단계 | 대상 | 명령 |
|---|---|---|---|
| 5-1 | LSTM 공실 예측 → 예측 레이어 | `ml/models/lstm/vacancy_lstm.py` | `/ml-train` |
| 5-2 | GNN 업종 추천 → 상권 AI 추천 | `ml/models/gnn/industry_gnn.py` | `/platform` |

## Phase 6 — 나머지 PPPP
| # | 단계 | 상태 | 명령 |
|---|---|---|---|
| 6-1 | Posting — 입점 솔루션(비용-효용 분석) | ◐ 어댑터 미연동이나 **폴백 입력 실데이터화 완료**(2026-08-01) | `/posting` |
| 6-2 | Program — 마케팅 자동화(LLM 콘텐츠) | ✅ 2026-08-01 실호출 검증 | `/program` |

### 가로수길 4개 섹션 실측화 (2026-08-01) — 2/4 완료, 1 부분, 1 막힘

거점 심층 뷰의 4개 섹션을 실측으로 채우는 작업. 거점 수를 늘리기(6번)보다 1거점을
깊게 채우는 쪽이 사업화 평가에 값이 크다는 판단으로 착수했다.

| 섹션 | 상태 | 근거 |
|---|---|---|
| 공실 히트맵 | ✅ 실측 | Gold 건물 마스터(3-1) |
| 상권 행사 | ✅ **실측 전환** | 서울열린데이터광장 문화행사 API |
| 공실 유닛 | ◐ 임대료 실측 + **실제 유닛 Gold 산출(미배선)** | R-ONE(6-1) + 건물 마스터 |
| 감성 구역 | ❌ **막힘** | 리뷰 원문 0건 (아래 참조) |

**행사 — 실측 전환 완료.** 신규 `data/collectors/seoul_events.py` +
`data/pipelines/build_events.py` → `gold/platform_events.json`, 서빙은
`app/services/events.py`. 전국 19,477건에서 종료되지 않은 행사만 거점 반경 1.5km 로
잘라 52거점 785건 배정.

⚠️ **커버리지 한계**: 이 API 는 공공·문화시설 행사 중심이라 **상업 상권의 팝업·플리마켓을
거의 담지 않는다.** 도심은 풍부하지만(cityhall 86 · ikseon 78 · myeongdong 72)
**가로수길은 2건뿐이고 둘 다 800m 밖**(논현 평생학습센터 모집, 재즈클럽 공연)이다.
빈 거점은 시드로 채우지 않고 빈 상태를 표시한다 — 채우면 지어낸 행사를 지도에 다시 찍는 셈이다.

⚠️ **시드에서 버린 것**: 시드 행사가 달고 있던 효과 지표(`k2` "유입 +52%", "전환 14%")·
이해관계자 역할(`roles`)·HA 메모(`ha`)는 근거 없이 적은 값이라 실데이터로 승계하지 않았다.
대신 API 가 주는 사실(장소·주최·기간·요금·대상·링크·거리)을 싣는다.
`test_events_carry_real_fields_not_fabricated_metrics` 가 이 규칙을 고정한다.

**공실 유닛 — Gold 산출 완료, 배선 미완.** `data/pipelines/build_vacant_units.py` 로
실제 공실 건물에서 유닛을 뽑았다(13거점 × 12유닛). 가로수길 예: 문정빌딩 24평,
용호빌딩 62평 — 이름·좌표·층수·업종·공실률이 실측이고 **면적은 건축물대장 상업면적
÷ 호실 수**다. 대장 면적이 없는 건물은 제외했다(면적을 가정하면 그 위 투자비·매출이
전부 가정이 된다).
**아직 `/postings` 는 시드 유닛을 쓴다 — 근거 부재로 보류다.** 실제 건물 유닛에는
`tier_scenarios()` 가 요구하는 셋이 없는데, **무엇으로 대신할지 정할 자료 자체가 없다**
(결정을 미룬 게 아니다):

| 입력 | 부재의 성격 | 해제 조건 |
|---|---|---|
| `prem` 권리금 | **데이터 부재** — 공개 통계 없음. 0 이면 초기투자 과소, 임의값이면 그 위 ROI 가 전부 가정 | 권리금 소스 확보, 또는 "시뮬레이션이 권리금을 포함해야 하는가"라는 제품 판단 |
| `foot` 유동인구 | **공간 해상도 부재** — flpop 은 거점 단위, 거점 안을 구분할 데이터가 없음 | ~~생활인구 50m 격자 또는 골목상권 10m 도로 단위~~ → **정정(2026-08-02 실사)**: 둘 다 공개되지 않는다. 실재하는 최소 단위는 **집계구**이고, 생활인구는 파일 다운로드 전용 + **경계는 SGIS 자료신청(승인 절차)**. 코드가 아니라 신청이 선행이다 → [finding-foot-traffic-resolution.md](finding-foot-traffic-resolution.md) |
| `rec` 추천 Tier | **기준 정의 부재** — "추천"이 회수 최단인지 순익 최대인지 리스크 최소인지 정의된 적 없음 | 추천 기준의 제품 정의 |

근거 없이 배선하면 "실제 건물"이라는 겉모습에 가정값 셋이 올라타 **오히려 시드보다
위험해진다** — 실측처럼 보이는 추정치가 가장 나쁘다. 그래서 산출까지만 하고 멈췄다.
상세는 파이프라인 모듈 주석 참조.

### 5번(감성 프록시 제거) — **수집에서 막힘, 미착수** (2026-08-01 판정)
모델링 문제가 아니라 **원천 데이터가 없다.** 착수 전 전수 조사 결과:

| 후보 소스 | 실태 |
|---|---|
| `data/crawlers/review_crawler.py` | `raise NotImplementedError` — 골격만, 수집 0건 |
| 네이버 블로그 17,653건(54거점) | 검색 API **스니펫**이고 내용이 해시태그 도배 광고성 포스팅. 거점 단위라 구역 좌표가 없다 |
| `kakao_places.json` | 점포 메타(이름·카테고리·좌표) — 리뷰 텍스트 아님 |
| `ml/` | 감성 모델 없음 |
| `gold/` | 감성 산출물 없음 |

**시드가 요구하는 것은 324개 구역(54거점 × 6구역) 단위 감성**이다. 블로그 코퍼스는
거점 단위라 구역으로 내릴 방법이 원천적으로 없고, 광고 포스팅에 감성분석을 돌리면
"광고가 얼마나 긍정적인가"를 재게 된다(게다가 이 코퍼스는 타 시도 지명 오염도 확인됨).

**Gold 의 실데이터로 대체하면 안 되는 이유**: `opbiz_rt`·`clsbiz_rt`·`selng_amt`·
`flpop`·`vac_*` 로 "상권 활력 지수"는 만들 수 있지만 그건 **감성이 아니다**. 활력 지표를
감성이라는 이름으로 바꿔 다는 건 시드를 두는 것보다 나쁘다.

**대신 한 일 — 없는 근거를 주장하지 않게 표기 교정.** 구역 카드가 "리뷰/SNS 구역별 감성",
"리뷰 2,140건" 이라고 적어 실제로 센 것처럼 읽혔다(센 적 없다). → 섹션 부제를
"전부 추정치다 — 리뷰 수집 미착수", 카드에 `추정` 배지, "리뷰 N건" → "가정 표본 N건",
KPI 설명을 "추정치 — 리뷰 수집 미착수"로 바꿨다. `/sentiment` 엔드포인트 docstring 에도
판단 근거를 남겼다.

**해제 조건**: 구역 단위 리뷰 원문 확보. 경로는 둘 —
① 점주 제공 데이터(B2B 온보딩 동의) — 상용 원칙이자 약관상 안전 ②
플레이스 리뷰 크롤러 구현(PoC 내부 검증 한정, 약관·저작권 리스크 — docs/feature-program.md §0).
어느 쪽이든 **수집 결정이 먼저이고 코드는 그 다음**이다.

### 6-1 결과 — Posting 폴백 입력 실데이터화 (2026-08-01)
외부 코파일럿 명세가 아직 없어 실제로는 **항상 3-Tier 폴백**이 돈다. 그래서 어댑터를
기다리는 대신 **폴백의 입력**을 실데이터로 올렸다. 신규 파이프라인
`data/pipelines/build_posting_inputs.py` → `gold/platform_posting_inputs.json`,
서빙은 `apps/backend/app/services/posting_inputs.py`.

| 입력 | 소스 | 상태 |
|---|---|---|
| `rent` 월임대료 | R-ONE 소규모상가 임대료 × 면적 × **층 계수** | ✅ 실데이터 |
| `foot` 유동인구 | 서울 상권분석 유동인구 3분위 + **거점 내 시드 서열** | ◐ 혼합 |
| `area` 면적 | 없음 — silver `com_area_flr` 은 건물 단위지 유닛 단위가 아니다 | ⬜ 시드 |
| `prem` 권리금 | 없음 — 공개 통계 부재(bronze 전수 확인) | ⬜ 시드 |

**거점 단위 실데이터를 유닛에 내릴 때 두 번 보정이 필요했다.** 둘 다 "실데이터가 절대
수준을, 계수가 거점 내 구조를" 맡는 형태다.

- **층 계수** — R-ONE 소규모상가 임대료는 사실상 1층 기준이다. 시드 270유닛과 대조하니
  R-ONE환산/시드 비율이 층수를 따라 단조 증가했다(1F 1.69 · 2F 3.66 · 3F 3.83 · 4F 5.25 ·
  6F 6.84 · 7F 8.17). 그대로 곱하면 상층 임대료를 2~8배 과대계상한다. 계수 적용 후 층별
  배수가 1.5~2.0으로 평탄해졌다. **이 계수는 실측이 아니라 계수다.**
- **거점 내 서열** — flpop 은 거점 단위라 그대로 내리면 한 거점의 모든 유닛이 같은 등급이
  된다(가로수길 고/고/중/저/중 → 전부 중, 등급 변경 179/270유닛). flpop 3분위로 거점
  기준 등급을 정하고 시드의 거점 내 상대 위치로 ±1칸 이동시키니 구조가 보존됐다(변경 111/270).

**출처 표기 필수**: 응답의 `inputs_source` 가 필드별 출처(`rone`/`flpop+seed`/`seed`)를
밝히고, 프론트 유닛 카드가 `R-ONE`·`유동인구`(초록) / `추정`(회색 점선) 배지로 노출한다.

**음성 대조**: 층 계수를 전부 1.0 으로 무력화하니 `test_rent_falls_with_floor` 가
`garosugil 3F 평당 24.5 >= 1F` 로 정확히 실패했다.

⚠ **함께 고친 선행 버그 — 회수기간 단위 혼용**: `tier_scenarios.roi()` 가
`invest(백만원) / net(만원)` 이라 회수기간이 100배 작게 나왔다(화면에 "회수 0개월 /
0.1개월"). `invest × 100 / net` 으로 교정 → 1.4~29.8개월. 실데이터를 넣은 바로 그
계산식이라 함께 처리했다.

⚠ **함께 고친 성능 문제 — 상권 콘텐츠 LLM 무캐시**: 크레딧 충전으로 LLM 경로가 살아나면서
`/marketing/{id}` 가 **호출마다** LLM 을 쳤다(실측 12~17초 + 매번 크레딧). 거점 심층 뷰가
이 응답을 기다려 화면이 멈췄다. 입력이 Gold 컨텍스트 파일 하나뿐이라 mtime 기준 캐시를
붙였다 → 첫 호출 17.6초, 이후 **0.03초**. 테스트 격리용 `clear_district_cache()` 공개.

### 6-2 결과 — Program LLM 실호출 검증 (2026-08-01)
크레딧 충전 후 가게 단위·상권 단위 LLM 경로를 처음으로 실호출로 검증했다.
모델은 `claude-opus-4-8`(config 기본값), `source == "llm"` 확인.

**핵심 교훈 — 폴백이 있는 코드에서 "테스트 통과"는 실호출의 증거가 아니다.**
기존 `test_posting_marketing.py` 는 `_call_llm` / `_call_district_llm` 을 통째로 목킹해
응답 **매핑**만 검증했다. 실호출이 깨져도 전부 `except` 에 잡혀 폴백으로 흐르고 200 이 났다.
모델 ID 를 고의로 깨뜨린 음성 대조 결과:

| 스위트 | 정상 | 모델 ID 고장 |
|---|---|---|
| 기존 목킹 (`test_posting_marketing.py`) | 10 passed | **10 passed** ← 고장에 눈이 멂 |
| 신규 라이브 (`test_llm_live.py`) | 4 passed | **4 failed** (`anthropic.NotFoundError`) |

**신규 테스트** `apps/backend/tests/test_llm_live.py` — 폴백을 끄고 계약을 친다.
기본 스위트에서는 skip, opt-in 이 필요하다(외부 호출·크레딧 사용):

```powershell
$env:SPACEOS_LIVE_LLM=1; py -3.11 -m pytest tests/test_llm_live.py -v
```

응답이 비결정적이라 문구가 아니라 **계약**만 단언한다 — 스키마 충족, 건수 범위,
한국어 여부, 해시태그 2개 규칙, `source != 폴백`, 행사 3건 시드 유지.

⚠ **입력 데이터 오염 발견**: 상권 컨텍스트의 `blog_keyword` 에 타 시도 지명이 섞여 있다
(54거점 중 5곳 — garosugil `창원(252)`, cityhall `대전·부산·수원`, nonhyeon `인천(60)`,
jangan·kyunghee `수원`). 불용어(`후기`·`추천`·`좋은`)와 인명(cheongdam `조재범`)도 올라온다.
이번 실행에서 LLM 은 창원을 알아서 무시했지만 보장이 아니다. 고칠 위치는 **수집/Gold 단계의
질의 정제**지 서빙 계층 denylist 가 아니다.

---

## 크리티컬 패스 (데이터가 지도에 뜨는 최단 경로)
```
(A)키발급 → 1-1·1-4 수집 → 2-2·2-3 가공 → 3-1 API → 4-2 지도 레이어
```
그 외 병렬: **4-1(지도 확대)은 지금 즉시**, Phase 1 각 데이터는 서로 독립 수집.

## (A)·(B) 위치 요약
- **(A)** = 0-3. Phase 1 전체의 전제 조건 → **가장 먼저**.
- **(B)** = 4-1. 데이터와 독립 → **(A)와 병렬로 지금 시작 가능**.
