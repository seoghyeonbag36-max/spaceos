# 예비군 3일 — 폰 작업 프롬프트 (2026-09-15 판)

> 09-08 판 [prompts-mobile-2026-09-08.md](prompts-mobile-2026-09-08.md) 의 **다음 판**이다.
> 두 경로(Claude 앱 / claude.ai/code)와 클라우드에서 안 되는 일은
> [../PlaceOS_Mobile_Dispatch_Prompts.md](../PlaceOS_Mobile_Dispatch_Prompts.md) §0 을 따른다.
> 이 판의 수치는 **2026-09-15 실측**이다(문서가 아니라 파일·grep 으로 셌다).

**이번 3일의 조건이 평소 폰 작업과 다른 점 셋**
1. **쓸 수 있는 시간이 휴식 10분·점심·저녁뿐이다.** 세션이 되물으면 다음 휴식까지 멈춘다
   → 모든 B 프롬프트 끝에 "되묻지 말고 보수적으로 고르고 PR 에 적어라"를 넣었다.
2. **프로덕션을 고칠 수 없다.** main 푸시는 곧 Cloud Run 배포다(`deploy.yml` 이 `branches: [main]`,
   경로 필터 없음) → **3일 동안 main 머지 0건.** PR 만 쌓는다.
3. **공모전 마감 두 개가 이 주에 있다**(§2). 3일의 무게중심은 코드가 아니라 원고다.

---

## 0. 오늘 밤 데스크톱에서 — 출발 전 10분

### 0-1. 화면설계서 3판은 이미 main 에 있다 ✅

PR #25 가 2026-09-14 23:07(KST) 에 머지됐다 — `main` HEAD `eea239c`, 그 푸시의 CI·Deploy 둘 다 성공.
09-15 에 프로덕션을 직접 확인했다: 번들에 「내 사업」·`industry-fit` 이 있고
`GET /api/v1/ai/industries` 가 200. **B 프롬프트는 `main` 기준 그대로 쓴다.**

> ⚠ 이 판 초안은 "3판이 main 에 없다"고 적었었다 — 틀렸다. 로컬 `git fetch` 가
> `remote helper 'https' aborted session` 으로 실패해 **낡은 로컬 `origin/main`** 과 비교했기 때문이다.
> 원격 상태를 판단할 때는 `gh api repos/seoghyeonbag36-max/spaceos/commits/main` 처럼 **gh 로 직접** 본다.

```bash
git status --short     # tsbuildinfo · reports/*.json · docs/papers/library/ 는 클라우드에 필요 없다 — 둬도 된다
```

### 0-2. 폰 준비 (각 1분)

- [ ] claude.ai/code 로그인 · 저장소 `seoghyeonbag36-max/spaceos` 연결 확인
- [ ] GitHub 모바일 앱 로그인 — PR 요약 읽기·코멘트용
- [ ] 이 문서를 폰에서 열 수 있게 둔다 (보낸 파일 또는 GitHub)
- [ ] 메일 앱에서 파일 첨부 발송이 되는지 — 01 은 **이메일 제출**이다
- [ ] 손택스(홈택스 모바일) 로그인 — 02 첨부 서류(국세청 사실증명)를 폰에서 받을 수 있는지 본다

노트북은 **꺼도 된다.** 이 문서의 작업은 전부 클라우드 세션·폰 앱에서 끝난다.

---

## 1. 3일 동안의 규칙

| 규칙 | 이유 |
|---|---|
| **main 머지 0건** — 원고 PR 도 머지하지 않는다 | 머지 = 배포. 제출은 PR 브랜치의 파일로 한다 |
| **클라우드 세션은 동시에 2개까지** | 휴식 10분에 PR 셋을 읽을 수 없다. §3 표의 짝끼리는 파일이 겹치지 않는다 |
| **부대 안에서 사진·화면 캡처를 첨부하지 않는다** | 09-06 판 A4-a(레퍼런스 해부)는 3일 동안 쉰다. 휴대폰 규정은 부대 안내가 우선이다 |
| **수정 지시는 새 세션이 아니라 같은 세션에 이어서** | 새 세션은 앞 세션이 읽은 것을 처음부터 다시 읽는다 |

---

## 2. 무엇이 급한가 — 마감

| | 대회 | 마감 | 제출 방법 | 원고 상태 (09-15 실측) |
|---|---|---|---|---|
| **01** | 제8회 공간정보 활용·아이디어 경진대회 | **2026-09-18** | 이메일 `vworld@spacen.or.kr` · 파일명 `참가자명-제목` | FILL **5** 남음 · 서식 미대조 · §5 수요자가 **옛 고객**(출점팀·자산운용사·지자체) |
| **02** | 2026 GovTech 창업 경진대회 | **2026-09-21 10:00** (실질 09-20 밤) | k-govtech.kr 온라인 + 국세청 사실증명 | FILL **3** 남음 · 서식 미대조 · 옛 고객 서술 7곳 |

`check_application.py` 는 두 원고 모두 위반 0 이다 — **틀린 문장은 없고, 빈 절과 옛 고객이 남았다.**

⚠ **예비군 기간이 18일(금)을 포함하면 01 의 제출일이 훈련 중이다.**
1일차에 원고(B17)를 끝내고 2일차에 제출 파일(B19)을 만들어야 3일차 휴식시간에 메일로 낸다.

### 2-1. 주최처 전화 2통 — 1일차 휴식시간 (평일 업무시간)

서식 대조가 두 원고 모두 막혀 있고, 이건 프롬프트가 아니라 전화로만 풀린다.

**01 공간정보산업진흥원 031-606-2562**
1. 주최 서식(참가신청서) 없이 자유 양식 원고(docx/pdf)를 내도 되는가 — 안 되면 서식은 hwp 인가
2. 복수 부문 출품이 가능한가
3. 동의서 3종(참가·개인정보·저작재산권)의 서명 방식 — 스캔? 전자서명?
4. 첨부 용량·형식 제한

**02 GovTech 사무국 02-726-1096**
1. 재직 중인 예비창업자가 참가 자격이 되는가
2. 사업계획서 서식은 어디서 받는가 · 자유 양식 허용 여부
3. 사업자등록이 없는 경우에도 사업자등록사실여부 사실증명을 발급해 내는가

답을 받으면 원고 머리의 `<!-- 확인필요(서식) -->` 주석에 적힌 질문과 대조해, 해당 B 세션에 한 줄로 이어서 알려 준다.

---

## 3. 3일 시간표

| | 아침 (출발 전) | 휴식 10분 | 점심 | 저녁·퇴근 후 |
|---|---|---|---|---|
| **1일차** | **B17 + B18** 걸기 | §2-1 전화 2통 | B17·B18 PR 맨 위 5줄 확인 | **A12** 인터뷰 키트 |
| **2일차** | **B19**(01 제출 파일) + **B16′** 걸기 | PR 확인 · 전화 답을 세션에 전달 | **A13** — 01 원고 심사위원 읽기 → 고칠 점을 B17 세션에 이어서 | **A14** 03·04 자격 판정 |
| **3일차** | **B20** + **B14′** 걸기 | **01 제출** (마감일이면) | **A13** — 02 원고 | 복귀 뒤 머지 순서 메모 (§6) |

동시에 거는 짝(B17·B18 / B19·B16′ / B20·B14′)은 서로 만지는 파일이 겹치지 않는다.

---

## 4. 먼저 — 09-08 판 중 무엇이 끝났나

`git log` 가 아니라 **파일·grep** 으로 셌다(2026-09-15).

| 09-08 번호 | 상태 | 근거 | 이번 판에서 |
|---|---|---|---|
| A5 도메인 | ✅ | `placeos.web.app` 정식 주소(09-13) | — |
| A7 파일럿 아웃리치 | ⛔ **폐기** | 대상 3군(출점팀·자산운용사·지자체)이 09-13 주요 고객에서 빠졌다 | **A12** 로 대체 |
| A8 앵커 60초 답변 | ♻ 유효 | 판정 문서 그대로 | 그대로 쓴다 |
| A9 Lovable 랜딩 | ❓ | `apps/landing/` 없음 — 미편입 | 3일 범위 밖 |
| A10 데모 대본 | ♻ **재작성 필요** | 서울·거점 탭이 사라지고 「내 사업」 설정이 생겼다 | 복귀 후 |
| B9 계정 화면 | ⬜ 미착수 | `pages/` 에 Signup·Login 없음 · 설계서 3판도 "미해결"로 뒀다 | **폰으로 안 보낸다** — 화면 결정이 먼저다 |
| B10 글자 정리 | ❌ 더 역행 | PlatformConsole 989 → **1,236줄** · ProgramStudio 772 → **1,165줄** | 줄 수로 자르지 않는다. 화면 정본이 3판 설계서가 됐다 → **B20** 감사로 대체 |
| B11 4P 플로우 | ✅ | 커밋 `4c880a5` "트랙 간 인계" | — |
| B12 "12개뿐?" | ❓ | 화면이 지도 전체화면으로 바뀌어 여전히 유효한지 미확인 | 3일 범위 밖 |
| B13 커버리지 두 눈금 | ⬜ | 대상 파일 `HubExplorer.tsx` 가 사라졌다 | 3일 범위 밖 |
| B14 하드코딩 색 | 🔶 거의 다 | hex 388 → **342** · var 543 → **1,120** (채택률 **76.6%**, 목표 80%) | **B14′** 재발행 |
| B15 논문 방법 근거표 | ⬜ | `method-evidence-table.md` 없음. 원문 `docs/papers/library/` 가 **커밋 안 됨** → 클라우드에 원문이 없다 | 데스크톱 |
| B16 앵커 라벨 | ⬜ 미착수 | `gold_vacancy.py:233` 이 여전히 `avg − anchor` · `aligned_gap` 0건 | **B16′** 재발행 (대상 파일이 바뀌었다) |

---

# A군 — 폰만으로 완결 (Claude 앱)

## A12 · 창업자 인터뷰 키트 — 1일차 저녁 30분

> **왜 지금**: 주요 고객이 창업자·업종 바꾸기/상권 옮기기 사업자로 바뀌었는데(09-13),
> 그 사람들을 **한 명도 만나지 않았다.** KPI②(유료 전환 의향 30%+)를 재려면 인터뷰가 먼저다.
> 복귀 후 2주 안에 5명을 만날 준비를 부대 안에서 끝낸다.

```
너는 PlaceOS 창업자의 고객 인터뷰 설계 파트너다. 2주 안에 5명을 인터뷰할 키트를 만든다.

[제품 한 줄] 서울 66거점의 건물·층 단위 공실을 건축물대장으로 실측하고,
내 업종 기준으로 상권 순위 → 빈 자리(층·거리뷰) → 입점 회수기간(3-Tier) → 홍보 방법까지 잇는 지도 서비스.
주소: placeos.web.app

[인터뷰 대상 — 둘을 따로 설계한다]
 ① 업종이 정해진 예비 창업자 (예: 카페·음식점을 준비 중)
 ② 지금 가게를 하면서 업종을 바꾸거나(피벗), 같은 업종으로 상권을 옮기려는 사업자

[낼 것]
 1. 모집 문구 — 온라인 커뮤니티용 200자 / 지인에게 소개 부탁하는 메시지 100자. 사례비는 [금액 미정]
 2. 30분 질문지 — 과거 행동만 묻는다
    예: "마지막으로 자리를 알아봤을 때 무엇을 열어봤고, 누구에게 물었나"
 3. 대상별로 확인할 가설 3개와, 각 가설이 틀렸다고 판정할 답변 예
 4. 마지막 10분에 화면을 보여주는 순서 — 「내 사업」 설정 → Platform → Page → Posting → Program.
    각 화면에서 "말하지 말고 지켜볼 것" 한 줄
 5. 기록 양식 — 인터뷰 1건 = 표 1장
    (문제 강도 1~5 · 지금 쓰는 대체재 · 그 일에 돈을 낸 경험 · 소개받을 수 있는 사람)

[금지]
 - "이런 기능 있으면 쓰시겠어요?" 같은 가정 질문
 - 유료 의향을 직접 묻는 질문 — 지금 무엇에 돈을 쓰는지 묻는다
 - 위 한 줄 밖의 수치 · "AI 기반"·"혁신적인" 같은 수식어
 - 사람을 지어내지 마라. 대상은 역할로만 적는다
```

## A13 · 원고 심사위원 읽기 — 2일차·3일차 점심 15분

> B17·B18 PR 의 원고를 GitHub 앱에서 복사해 **맨 아래에 붙여넣는다.**
> 여기서 나온 수정안은 원고에 직접 옮기지 말고 **B17/B18 세션에 이어서** 보낸다 — 거기서 검사기가 돈다.

```
아래 붙여넣는 공모전 원고를 심사위원 입장에서 읽는다.

[대회 — 하나를 남기고 지운다]
 (01) 공간정보 활용·아이디어 경진대회. 배점: 구체성 10 · 타당성 10 · 데이터 확보 용이성 20 ·
      실현성 20 · 완결성 10 · 활용성 10 · 지속성 10 · 우수성 10
 (02) GovTech 창업 경진대회. 심사축: 공공서비스 혁신 · 공공성 · 실현가능성 · 성장전략 · 팀

[낼 것]
 1. 배점 항목별 예상 점수(5점 척도)와 감점 이유 한 줄
 2. 심사위원이 30초 안에 못 찾을 핵심 문장 — 어느 절 맨 앞으로 옮기면 되는가
 3. 고칠 문장 5개 — 원문 / 수정안 / 이유
 4. 발표·질의에서 나올 질문 5개와 각 2문장 답

[금지]
 - 원고에 없는 숫자·실적을 제안하지 마라. 필요하면 [측정 필요]
 - "미평가"를 "높은 정확도"로 바꾸거나 빼는 제안 — 미평가는 미평가로 둔다
 - 수치에서 조건(예: "건축물대장 실측")을 떼는 제안 — 문장의 뜻이 바뀐다
 - (02) 에 M&A·매각 서사를 넣는 제안 — 공공성 심사에서 감점이다

[원고]
(여기에 붙여넣기)
```

## A14 · 다음 공모전 03·04 자격 판정 — 2일차 저녁 15분

> 웹 검색이 되는 앱에서 쓴다. `docs/apply/README.md` 가 둘 다 "자격 확인 먼저"라고 적어 두었다.

```
두 공모전의 공고 원문을 찾아 참가 자격을 판정한다. 판정마다 공고 URL 을 붙인다.

 03 제3회 미래융합인재 발굴 SW 챌린지 (마감 2026-10-07) — 일반부에 3~5인 팀·연령 요건이 있다고 알고 있다
 04 제9회 핀테크 아이디어 공모전 (마감 2026-10-08 16:00)

[나] 1인 예비창업자. 서비스: 서울 상권의 건물·층 단위 공실 실측 지도 + 업종 기준 입지·입점 회수기간·홍보 안내

[대회마다 낼 것]
 1. 자격 요건 원문 인용 + 1인 참가 판정 (가능 / 불가 / 문의 필요)
 2. 제출물 · 분량 · 서식 · 문의처
 3. (04만) PlaceOS 를 금융 언어로 다시 쓰는 각도 3개 — 예: 창업 대출 심사의 입지 리스크. 수치 없이

[금지] 공고를 못 찾으면 추측하지 말고 "공고 미확인"이라고 적어라
```

---

# B군 — 클라우드 세션 지시 (claude.ai/code → PR)

> 각 프롬프트는 **대상 파일 · 입력 근거 · 통과 조건 · 금지** 넷을 채웠고,
> 끝에 **[진행 방식]** 을 붙였다 — 휴식시간에만 보는 조건에서 세션이 멈추지 않게 한다.
> 기준 브랜치는 `main` 이다(3판 포함 — §0-1).

## B17 · 01 공간정보 원고 완성 ★1일차 아침 · 마감 09-18

```
docs/apply/01-spatial-info/draft.md 를 제출 가능한 원고로 채운다. 마감 2026-09-18.
main 에서 브랜치 feat/reserve-b17-spatial-draft 를 만든다.

[먼저 읽을 것 — 이 순서]
 1. docs/apply/AGENTS.md — "절대 하지 않는 것" 6개
 2. docs/apply/claims.json — allow 의 id 와 forbid
 3. docs/apply/README.md
 4. docs/screen-spec-pppp-2026-09-13.html 의 「주요 고객」 절과 「내 사업」 공통 설정
 5. apps/backend/app/services/business_fit.py 의 업종 12종 표

[할 일]
 1. FILL 마커 5개(§1 · §2 · §4 · §5 · §6)를 마커의 지시·분량대로 채운다
 2. §5 는 마커부터 고친다. 이 판단은 이미 내렸다(2026-09-13 주요 고객 재정의):
      수요자 ① 업종이 정해진 예비 창업자
             ② 지금 상권에서 업종을 바꾸거나, 같은 업종으로 상권을 옮기려는 사업자
      지자체는 "상권 활성화·소상공인 지원 창구가 같은 화면을 쓸 수 있다"는 활용 경로로만 한 줄
      프랜차이즈 본사·자산운용사는 쓰지 않는다. 건물주 연결은 "추후 목표"로만
    마커의 "수요자 셋" 문장을 위 기준으로 바꾼 뒤 본문을 채운다 — 마커가 본문의 정본이다
 3. 이미 채워진 절(§3 · §7 · §8 · §9)에 옛 고객(출점팀·자산운용사) 서술이 있으면 같은 기준으로 고치고
    고친 문장을 PR 본문에 원문/수정 표로 적는다
 4. §4 의 화면 캡처 5~8컷은 여기서 못 찍는다(네이버 지도 키가 없다).
    컷마다 「무엇을 보여주는가 · placeos.web.app 에서 어떤 조작으로 여는가」를 표로 적고
    본문에는 [캡처 N] 자리표시만 둔다. 화면 이름은 설계서 3판 기준으로 쓴다
 5. 원고 머리의 <!-- 확인필요(서식) --> 주석은 지우지 않는다 — 서식 대조는 사람이 한다

[통과 조건]
 - python scripts/check_application.py --doc docs/apply/01-spatial-info/draft.md --require-complete 가 exit 0
 - 절별 글자 수(공백 포함) 표 — 마커 분량 범위 안
 - 본문의 수치마다 claims.json allow id 가 무엇인지 절별 대응표

[금지]
 - claims.json 을 고치지 마라. B18 세션이 동시에 돈다. 필요한 수치가 대장에 없으면
   쓰지 말고 <!-- 확인필요: … --> 로 남긴다
 - 파일럿·고객사 실적을 성과로 쓰지 마라 — 목표로만
 - 서빙 보류 거점(고양·파주)을 66 에 더하지 마라
 - "AI 기반"·"혁신적인"·"원스톱" 같은 수식어
 - docs/papers/ · docs/feature-*.md · reports/*.json 을 고치지 마라

[진행 방식] 나는 폰으로 휴식시간에만 본다. 되묻지 말고 끝까지 가라.
 판단이 갈리면 덜 바꾸는 쪽을 고르고 PR 본문 「판단 필요」 절에 적는다.
 PR 본문 맨 위는 5줄 요약(무엇을 했나 / 통과 조건 결과 / 판단 필요 개수) — 폰 한 화면에 들어가야 한다.
 main 에 머지하지 마라.
```

## B18 · 02 GovTech 원고 완성 ★1일차 아침 · 마감 09-21 10:00

```
docs/apply/02-govtech/draft.md 를 제출 가능한 원고로 채운다. 마감 2026-09-21 10:00.
main 에서 브랜치 feat/reserve-b18-govtech-draft 를 만든다.

[먼저 읽을 것 — 이 순서]
 1. docs/apply/AGENTS.md  2. docs/apply/claims.json  3. docs/apply/README.md
 4. docs/screen-spec-pppp-2026-09-13.html 의 「주요 고객」 절
 5. 원고 머리의 "M&A Exit 서사를 쓰지 않는다" 주석

[할 일]
 1. FILL 마커 3개(§1 문제인식 · §3 성장전략 · §4 팀)를 마커의 지시·분량대로 채운다
 2. 고객 기준을 2026-09-13 재정의로 옮긴다:
      주사용자 = 업종이 정해진 예비 창업자 · 업종을 바꾸거나 상권을 옮기려는 사업자
      지자체는 "행정이 공실을 사후에만 안다"는 공공 수혜자·배포 창구로는 남겨도 된다 — 주사용자로 쓰지 않을 뿐이다
      프랜차이즈 본사·자산운용사는 주사용자로 쓰지 않는다
    §3 마커의 "고객군" 문장부터 이 기준으로 바꾼 뒤 채운다. 이미 채워진 §2 · §5 · §6 의 옛 고객 서술도
    같은 기준으로 고치고 원문/수정 표를 PR 본문에 적는다
 3. 수익 모델은 저장소 문서에 적힌 것만 쓴다. "창업자 대상 과금"이 어디에도 정의돼 있지 않으면
    지어내지 말고 <!-- 확인필요: 창업자 대상 과금 체계 미정 --> 로 남기고 「판단 필요」 에 올린다
 4. §4 팀: 1인이라는 사실을 감추지 않는다(마커의 각도를 따른다). 이름·경력을 지어내지 마라 — [이름] 자리표시
 5. 원고 머리의 <!-- 확인필요(서식) --> 주석은 지우지 않는다

[통과 조건]
 - python scripts/check_application.py --doc docs/apply/02-govtech/draft.md --require-complete 가 exit 0
 - 절별 글자 수 표 · 수치별 claims.json allow id 대응표

[금지]
 - claims.json 을 고치지 마라 (B17 과 동시에 돈다)
 - M&A·포털 매각 서사 — 같은 사실은 "공공·민간 플랫폼과의 데이터 연계 확장"으로 쓴다
 - 미평가를 성과로 고쳐 쓰거나 빼지 마라 · 파일럿 실적을 성과로 쓰지 마라
 - "AI 기반"·"혁신적인"·"원스톱" 같은 수식어

[진행 방식] 나는 폰으로 휴식시간에만 본다. 되묻지 말고 끝까지 가라.
 판단이 갈리면 덜 바꾸는 쪽을 고르고 PR 본문 「판단 필요」 절에 적는다.
 PR 본문 맨 위는 5줄 요약 — 폰 한 화면에 들어가야 한다. main 에 머지하지 마라.
```

## B19 · 01 제출 파일 (.docx) ★2일차 아침 · B17 PR 브랜치 위에서

> `[참가자명]` 과 `[제목]` 을 채워서 붙여넣는다. 주최처가 서식(hwp)을 요구한다고 답했다면
> 이 프롬프트 대신 **서식 항목명 목록을 받아 원고를 그 순서로 옮기는 작업**을 B17 세션에 이어서 보낸다.

```
docs/apply/01-spatial-info/draft.md 를 이메일 제출용 .docx 로 만든다.
브랜치 feat/reserve-b17-spatial-draft 위에서 작업한다(새 브랜치 금지 — 원고와 제출 파일이 한 PR 에 있어야 한다).

[먼저 읽을 것]
 scripts/build_business_plan_docx.py — 한글 폰트 임베딩(Noto Sans KR subset → .odttf 난독화 →
 fontTable.xml 배선)이 이미 구현돼 있다. 새로 짜지 말고 그 함수를 재사용한다

[할 일]
 1. scripts/build_apply_docx.py 를 만든다 — 입력 원고 md 경로, 출력 docx 경로를 인자로 받는다
    <!-- --> 주석은 전부 빼고, [캡처 N] 자리는 회색 상자 + "캡처 N" 글자로 둔다
 2. 출력: docs/apply/01-spatial-info/submit/[참가자명]-[제목].docx
 3. 표·제목 위계를 원고 그대로 옮긴다. 단락 테두리는 top+bottom 만 쓴다(4면 지정은 OOXML 스키마 위반)
 4. 폰트를 내려받지 못해 임베딩이 실패하면 멈추지 말고, 임베딩 없는 판을 같은 폴더에
    ...-noembed.docx 로 따로 만들고 그 사실을 PR 본문 맨 위에 적는다

[통과 조건]
 - docx 가 zip 으로 열리고 word/fonts/ 에 .odttf 가 있다 (임베딩 판)
 - 원고의 절 제목 수 = docx 의 제목 단락 수 (스크립트로 세어 보고)
 - 본문에 "FILL" · "확인필요" · "<!--" 문자열이 0건
 - python scripts/check_application.py --doc docs/apply/01-spatial-info/draft.md 가 여전히 exit 0

[금지]
 - 원고 본문을 고치지 마라. 변환만 한다
 - 동의서 3종을 만들지 마라 — 주최 서식이 있어야 한다

[진행 방식] 나는 폰으로 휴식시간에만 본다. 되묻지 말고 끝까지 가라.
 판단이 갈리면 덜 바꾸는 쪽을 고르고 PR 본문 「판단 필요」 절에 적는다. main 에 머지하지 마라.
```

**제출 (3일차 · 폰)**: GitHub 앱 → PR 브랜치 → `submit/` 의 docx 내려받기 → 메일 앱에서
`vworld@spacen.or.kr` 로 첨부 발송. 동의서 3종은 전화로 들은 방식대로 붙인다.

## B16′ · 앵커 라벨 정렬 (재발행) ★2일차 아침

```
같은 거점의 공실률이 두 정의로 떠 있고, 격차가 정렬 안 된 값끼리 빼서 작게 나오는 것을 고친다.
main 에서 브랜치 feat/reserve-b16-anchor-labels 를 만든다.

[확인부터 — 먼저 보고하라. 판정 근거는 docs/finding-anchor-gap-2026-09.md §4-1 · §4-2]
 - apps/backend/app/services/gold_vacancy.py:233  "anchor_gap_pp": round(avg - anchor, 2)
   → 대표 공실률(avg)에서 앵커를 뺀다
 - apps/backend/app/services/vacancy_forecast.py:37 · ml/inference/predictor.py:41
   → 예측 계열은 rone_aligned.mid(R-ONE 과 같은 모집단)를 쓴다
 - 화면: apps/frontend/src/pages/MapShell.tsx:714-716 · PageDashboard.tsx:194 · :500 이 anchor_gap_pp 를 그린다
 - 전달: apps/backend/app/services/districts.py:374 · :417 · schemas/district.py:57 · :142 · src/lib/api.ts:70 · :200
 줄 번호가 어긋났으면 grep -rn "anchor_gap_pp\|rone_aligned" 로 다시 찾는다.
 (09-08 판의 대상 HubExplorer.tsx 는 삭제됐다)

[할 일 — finding 문서 §4-2 의 라벨 표를 구현한다. 라벨을 새로 발명하지 마라]
 주 지표   거점 전체 공실률 (실측·호실 기준)        ← avg
 대조 지표 중대형 상가 공실률 (R-ONE 정렬)          ← rone_aligned.mid
 앵커      R-ONE 중대형 (최신 분기)
 격차      정렬 격차 = 대조 지표 − 앵커             ← 주 지표에서 빼지 않는다
 폭        불확실 구간                              ← vacancy_floor_hi/lo_pct
 수치는 09-08 문서의 연남 값을 옮기지 말고 data/gold/<hub>/calibration.json 에서 지금 값을 읽어 보고한다

[통과 조건]
 - 기존 anchor_gap_pp 의 뜻을 조용히 바꾸지 않는다. 새 필드(예: aligned_gap_pp)를 추가하고
   필드 → 계산식 → 쓰는 화면을 표로 보고한다
 - 화면에서 "공실률"이라는 같은 이름으로 서로 다른 두 수가 뜨는 곳 0곳 — grep 결과를 붙인다
 - cd apps/backend && pytest 통과 · cd apps/frontend && npm run build 통과 · npx vitest run 통과
 - 값이 바뀌는 API 응답 키를 전부 나열한다

[금지]
 - 격차를 작아 보이게 만들지 마라. 크면 큰 대로 그린다
 - Gold 산출물을 다시 만들지 마라 — 클라우드에는 bronze/silver 가 없다. calibration.json 을 읽는다
 - 불확실 구간을 빼지 마라 · ml/ 예측 경로를 건드리지 마라 · 지도 생명주기(MapHost)를 건드리지 마라

[진행 방식] 나는 폰으로 휴식시간에만 본다. 되묻지 말고 끝까지 가라.
 판단이 갈리면 덜 바꾸는 쪽을 고르고 PR 본문 「판단 필요」 절에 적는다.
 PR 본문 맨 위는 5줄 요약. main 에 머지하지 마라.
```

## B20 · 설계서 3판 통과 조건 ↔ 코드 대조 감사 ★3일차 아침 · 읽기 전용

> B10(글자 정리)을 대신한다. 화면의 정본이 설계서 3판이 됐으니, 줄 수를 줄이기 전에
> **설계서가 요구한 것이 코드에 있는지**부터 센다. 코드를 안 고치므로 3일 조건에 가장 안전하다.

```
docs/screen-spec-pppp-2026-09-13.html(3판)의 통과 조건을 코드와 테스트에 하나씩 대조한다. 코드는 고치지 않는다.
main 에서 브랜치 feat/reserve-b20-spec-v3-audit 를 만든다.

[대상 — 설계서의 통과 조건 id 전부]
 C-01~C-11 · PL-01~PL-12 · PG-01~PG-10 · PS-01~PS-08 · PR-01~PR-08 (49개)
 3판에서 새로 생긴 C-08~11 · PL-08~12 · PG-09~10 · PS-08 · PR-08 을 먼저 한다

[조건마다 적을 것 — 한 행]
 id / 조건 원문 요약 한 줄 / 구현 위치(파일:줄) / 그것을 지키는 테스트 이름(vitest·pytest) /
 판정: 충족 · 부분 · 미충족 · 화면 필요(코드만으로 판정 불가)

[방법]
 - cd apps/frontend && npm ci && npx vitest run · cd apps/backend && pytest 를 실제로 돌려
   통과한 테스트 이름만 근거로 쓴다. 이름만 보고 "있을 것"이라고 적지 마라
 - 설계서 id 가 코드·테스트 이름에 직접 적혀 있으면(grep) 그것을 우선 근거로 쓴다

[산출물] docs/finding-screen-spec-v3-audit-2026-09.md — .claude/skills/probe-first 양식
 맨 위: 판정별 개수 표 + "미충족" 목록(복귀 후 작업 후보) + "화면 필요" 목록(데스크톱 /verify 몫)

[통과 조건]
 - 49개 전부 행이 있다. 빈 판정 0
 - 모든 "충족"에 파일:줄 또는 테스트 이름이 붙어 있다
 - 재현 명령을 문서에 적는다

[금지]
 - 코드·테스트·설계서를 고치지 마라. 이번은 판정만이다
 - 미충족을 발견해도 고치지 말고 목록에 올린다
 - 지도 픽셀·네이버 SDK 가 필요한 조건을 "충족"으로 적지 마라 — "화면 필요"다

[진행 방식] 나는 폰으로 휴식시간에만 본다. 되묻지 말고 끝까지 가라.
 판단이 갈리면 보수적인 판정(덜 충족 쪽)을 고르고 문서에 이유를 적는다. main 에 머지하지 마라.
```

## B14′ · 하드코딩 색 342 → 100 이하 (재발행) ★3일차 아침

```
하드코딩 색을 토큰으로 마저 옮긴다. 순수 리팩터다 — 픽셀이 움직이면 실패다.
main 에서 브랜치 feat/reserve-b14-color-tokens 를 만든다.

[실측 2026-09-15 — apps/frontend 에서]
 grep -oE "#[0-9a-fA-F]{3,8}" src/pages/*.css src/components/*.css src/App.css | wc -l   → 342
 grep -o "var(--" src/pages/*.css src/components/*.css src/App.css | wc -l              → 1120
 채택률 = var / (var + hex) = 76.6%.  목표: hex 100 이하 · 채택률 80% 이상
 (09-08 에는 388 / 543 / 58.3% 였다)

[할 일]
 1. 342개를 파일별로 세어 목록을 먼저 낸다
 2. src/styles/tokens.css 에 **같은 값의** 변수가 이미 있는 색만 var() 로 바꾼다
 3. 대응이 없는 색은 그대로 두고, 파일·값·개수를 표로 보고한다. 새 토큰은 제안만 하고 만들지 마라
 4. --track-platform/page/posting/program 과 값이 겹치는 자리는 표시만 한다

[통과 조건]
 - 위 두 grep 수를 작업 전/후로 보고
 - npm run build 통과 · npx vitest run 통과
 - 바꾼 모든 자리가 "같은 값의 이름만 바뀐 것"임을 hex → 변수명 대응표로 보인다

[금지]
 - .tsx 인라인 스타일과 지도 오버레이 색(MapShell.tsx 의 fillColor 등)은 건드리지 마라 — SDK 가 인라인으로 그린다
 - 새 색을 만들지 마라 · 비슷한 색을 하나로 합치지 마라(값이 다르면 픽셀이 움직인다)
 - Tailwind 를 설치하지 마라

[진행 방식] 나는 폰으로 휴식시간에만 본다. 되묻지 말고 끝까지 가라.
 판단이 갈리면 바꾸지 않는 쪽을 고르고 PR 본문 「판단 필요」 절에 적는다. main 에 머지하지 마라.
```

---

## 5. 폰으로는 못 하는 것 — 복귀 후 데스크톱

| 작업 | 왜 폰에서 안 되나 |
|---|---|
| 01 §4 화면 캡처 5~8컷 | 부대 안 캡처 금지 + 로컬 앱·네이버 지도 키 |
| 02 서식 옮기기 · 사실증명 첨부 · k-govtech.kr 제출 | 서식(hwp)이 전화 답에 달렸다. **09-20 밤까지** |
| B9 계정 화면 | 설계서 3판이 "미해결"로 둔 화면 결정이 먼저다 |
| B15 논문 방법 근거표 | 원문 `docs/papers/library/` 가 git 에 없다 |
| B20 의 "화면 필요" 목록 | 지도 픽셀 — `/verify` |
| A10 데모 대본 재작성 | 3판 화면을 직접 눌러 보고 쓴다 |

## 6. 복귀 후 머지 순서

1. PR 5줄 요약을 한 번에 훑고 **「판단 필요」부터** 답한다
2. 코드 PR 은 `/verify` 뒤에 — **B16′ → B14′** (둘 다 화면에 닿는다. B16′ 이 응답 키를 바꾸므로 먼저)
3. 문서 PR(B17 · B18 · B19 · B20)은 **한 번에 묶어** 머지한다 — 머지 한 번이 배포 한 번이다
4. 이 문서 §4 대조표를 다음 판에서 다시 센다
