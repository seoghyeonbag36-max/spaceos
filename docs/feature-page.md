# Page — 공실 히트맵 + 층별 매물 목록 + 네이버 거리뷰

> PPPP: **Product ▶ Page** — *이 platform 안에 어떤 page 가 만들어져야 하는가?* 제품·상품이
> 아니라 **page** 를 묻는다. 어떤 업장이 어디에 있고 어디가 비어 있는지를 신뢰도 높은 시각
> 인터페이스로 표현해 **page 가 놓일 자리**를 드러낸다. 공실 히트맵과 **건물 상세(2D 층 스택
> + 거리뷰)** 가 핵심.
>
> ⚠ **2026-09-05 — 전환원에서 Price 가 빠졌다.** 종전 `Product/Price → Page` 는 Page 가
> 가격까지 겸한다는 뜻이었는데, 가격대 판단은 **Posting(Price ▶ Posting)** 의 몫이다.
> Page 는 "어디에 자리가 있나"까지만 답한다. 정본은 `CLAUDE.md` §PPPP Framework.

## 0. 구현 현황

⚠ **2026-09-05 — 3D 디지털 트윈은 폐기됐다. 이 문서 아래쪽은 그 전에 쓰였다.**
`BuildingTwin`(@react-three/fiber)이 그리던 절차적 박스는 실측 형상이 아니라 **층 상태를
색으로 말하던 것뿐**이라, 2D 층 스택 + 네이버 거리뷰(`components/BuildingViewer.tsx`)로
갈았다(번들 832KB → 4KB). **3D 를 없앤 것이지 층 표현을 없앤 것이 아니다** — 층
근거(`com_floors`·`occ_floors`)를 가진 31,782동의 실측은 그대로 화면에 남는다.

경위와 무엇을 잃고 얻었는지는 `docs/feature-posting.md` **§0-V** 에 적혀 있다(같은 커밋이
Posting 의 업종 관측까지 건드려서 그쪽에 남았다). 아래 §0 이후에서 "트윈"을 읽을 때는
**2D 층 스택**으로 옮겨 읽고, `BuildingTwin` 이라는 이름이 나오면 이력이다.

- 지금 표면: `pages/MapShell.tsx` → `components/BuildingViewer.tsx` → `lib/naverMap.ts`
- ⚠ 거리뷰는 촬영 시점이 몇 년 전일 수 있어 **공실 판정의 근거가 아니다.** 그래서 촬영일을
  반드시 같이 그린다 — 안 밝히면 사용자가 눈으로 본 셔터를 현재 상태로 읽는다.

**층별개요 수집 종결 — 잔여 650동은 "회수할 것"이 아니다 (2026-08-19 · 08-20).**
`--only-approx` 로 50거점 672동에 재호출해 **22동, 4거점만** 회수했다
(nambu 8 · jangan 6 · gongdeok 4 · jamsilsaenae 4). 나머지 46거점은 회수율 **0.0%**.

**0% 는 실패가 아니다.** 응답없음 0 · 429 0 으로 층별개요를 전부 정상 수신했고, 그 응답에
**상업 층이 0** 이라 근거 없는 capacity 를 지어내지 않고 `floor_approx` 를 유지한 것이다
(07-26 교정 조항). 몇 번을 호출해도 안 바뀐다.

회수된 4거점을 `recalc_capacity` + `build_page_master` 로 재산출했다:

| 거점 | 대표 집계 공실률 | 커버리지 |
|---|---|---|
| nambu | 61.6% (268동) | 98.5% |
| jamsilsaenae | 41.1% (286동) | 96.6% |
| jangan | 43.6% (175동) | 96.7% |
| gongdeok | 50.4% (226동) | 100.0% — `floor_approx` 가 서빙 집합에서 완전히 소멸 |

🐞 **쿼터 672콜을 태워 22동을 얻었다 — 프리플라이트가 유도한 낭비다 (08-20 교정).**
"잔여 동수"는 회수 가능성과 무관한데 프리플라이트가 그것을 명령줄과 함께 찍었다.
그날 데이터를 시도 이력으로 가르면 결론이 하나로 떨어진다:

| 그날 시도한 672동 | 회수 | 판정(상업층 0) | 회수율 |
|---|---|---|---|
| 미시도였음 | 22 | 1 | **95.7%** |
| 이미 시도했었음 | 0 | 649 | **0.0%** |

미시도 95.7% 는 `quota.md` 08-17 표(94.3~96.2%)와 같은 값이고 기시도는 예외가 없다 —
**필터가 있었으면 23콜로 끝날 일이었다(29배).**

고침은 추정을 버리고 **직접 보는 것**이다. mtime 으로는 못 잰다 — `floor_capacity` 가 bronze
를 쓴 직후 같은 실행에서 gold 를 제자리 갱신하고 **회수가 0 인 거점도** `_persist` 를 타서,
08-19 실행이 46거점의 mtime 까지 올려놨다. 그래서 `bronze/<slug>/*/bldg_flr_raw.json` 을
날짜별로 합쳐 그 건물 `lnoCd` 가 들어 있는지로 시도 여부를 판정한다(`_tried`).
bronze raw 262MB 를 파싱해 11초 걸리지만 수천 콜을 가르는 판단이라 값을 한다.

현재 상태(`python scripts/quota_preflight.py`): **잔여 650동 / 50거점 — 미시도 0 · 판정완료
650.** 명령줄이 아예 안 찍힌다. **새 거점을 넣거나 대장을 새로 받기 전까지 층별개요로
회수할 것은 없다.**

**3D 트윈 층 실배치 — 연결 완료 (2026-08-17).**
트윈이 "몇 층이 비었나"를 **실측 층 번호**로 그린다. 종전에는 점유율만큼 **아래부터 채우는
근사**여서, 3층이 비고 1층이 찬 건물도 1층부터 찬 것으로 그려졌다.

원인은 데이터 부재가 아니라 **파이프라인이 층 번호를 버린 것**이었다. `build_page_master._aggregate`
는 상업층 집합(`capacity_floors`)과 점포 확인 층(`store_flr_nos ∪ 인허가 층`)을 이미 계산해
놓고 **개수만**(`active_floors_lo/hi`) 남겼다. 층 번호를 산출물에 실어 프론트로 흘린다.

| 새 property | 뜻 |
|---|---|
| `com_floors` | 상업 용도 층 번호 — 공실률의 **분모** |
| `occ_floors` | 점포(상가정보 flrNo)·인허가로 **확인된** 영업 층 — 분자의 하한 |
| `unknown_n` | 층 미상 점포로 빈 상업층에 배정된 층 수(상한 − 하한) |

트윈은 층을 4색으로 구분한다: 영업 확인(녹) · 층 미상 배정(황) · 공실(상태색) ·
**비상업 층(회, 좁게)**. 마지막이 특히 중요하다 — 종전 트윈은 주거·업무 층까지 상가인 양
그렸다. `com=[1,4,5]` 인 건물이면 2·3층은 분모 밖이라는 게 화면에 드러난다.

- 층 미상 점포를 앉히는 규칙은 파이프라인과 **동일**하게 맞췄다(빈 상업층에 낮은 층부터).
  어긋나면 트윈의 녹색 층 수와 카드의 `vacancy_rate` 가 서로 안 맞는다.
- 층 근거가 없는 건물은 **종전 근사로 폴백**하되, 캡션에 "아래부터 채운 근사"라고 밝힌다.
- 배선: `_aggregate` → gold 마스터 properties → `services/building_vacancy` 는 gold 를 그대로
  통과시키므로 **백엔드 변경 없음** → `MapShell` · `PageDashboard` → `BuildingTwin`
  (⚠ 2026-09-05 이후 이 끝점은 `BuildingViewer` 다 — 층 4색 규칙은 그대로 옮겨갔다).
- 재산출 영향: 54거점 coverage.json 이 전부 `built_at` 한 줄만 바뀌었다 — **수치 드리프트 0**.
  마스터 합계 29.7 → 30.7MB (+3.4%).

**커버리지 확대 — 41거점 recalc 반영 (2026-08-17).**
`capacity_floors` 를 남기는 `recalc_floor_ouln` 이 13거점에서만 돌아 있어 층 실배치가
**18.7%(8,515동)** 에 그쳤다. 나머지 41거점에 돌려 **58.1%(26,461/45,580동)** 로 넓혔다.
50% 이상 거점 12 → **43곳**, 0% 거점 **0곳**. 정합성 검사 26,461동 **위반 0**.

수치 영향(대표 집계 공실률, 40거점 변동): 중앙 **+2.6%p** · 평균 +2.4%p · 범위 −7.8%p
(chungmuro 26.4→18.6) ~ +15.9%p (garak 22.4→38.3). **0%/100% 로 붕괴한 거점 없음,
커버리지 하락 거점 없음.** 앵커는 `calibrate_vacancy` 로 54거점 재산출했다.

🐞 **`load_latest` 가 부분 수집본을 집는 결함 — 이때 드러났다.**
층별개요는 건축HUB 일일 쿼터 때문에 **날짜를 나눠 조금씩** 모은다. 그런데
`recalc_floor_ouln` 이 `common.load_latest`(가장 최근 날짜 폴더)로 원본을 읽어,
그날 받은 일부만 담긴 스냅샷이 앞서 모은 완전본을 **가렸다**.

| 거점 | 최신 스냅샷 | 실제 최대 | 1차 실행 결과 |
|---|---|---|---|
| dangsan | 08-17 **18**지번 | 08-08 160지번 | 공실 19.5% → **0.0%** |
| mullae | 08-17 **60** | 08-15 548 | 15.5% → **0.0%** |
| yongsan | 08-17 **51** | 08-08 279 | 15.8% → **1.0%** |
| chungmuro · anam · samcheong | 〃 | 〃 | −10.5 ~ −7.8%p |

분모(상업층)가 비면 층 매칭이 안 붙어 레거시로 폴백하고, `active > capacity` 가 되며
occupancy 가 1.0 으로 포화된다 → **공실률 0% 아티팩트**. 이 문서 상단이 경고하는
"측정값이 아니라 아티팩트"와 같은 실패 양상이 입력 선택 단계에서 재발한 것이다.

고침: `recalc_floor_ouln._load_flr()` 이 **전 스냅샷을 병합**한다(같은 지번은 층 레코드가
많은 쪽을 남긴다 — `build_building_attrs` 와 동일 규칙). `build_building_attrs` 는 이미
`glob("*/bldg_flr_raw.json")` 로 전 스냅샷을 순회하고 있었다. 재실행은 **멱등**임을
dangsan 으로 확인했다(두 번 돌려 13.2% 동일).

⚠ 같은 결함이 `load_latest` 를 쓰는 **다른 소비 지점에도 잠재**한다(`stores_raw.json` ·
`bldg_polygons.geojson` 등). 부분 수집이 일어나는 소스인지 확인 후 같은 병합을 적용할 것.

**부수 효과 — 앵커 49 → 54/54.** `calibrate_vacancy` 재산출로 종전 5곳(kyunghee 등)에
없던 `calibration.json` 이 생겼다. 이로써 `test_gold_anchor_comparison_attached` 의
"calibration.json 없음" 실패(기존 결함)가 해소됐다.

🔴 **미해결 — nokdu 앵커 격차 34.55%p 로 가드레일(30) 초과.** 같은 테스트가 이제
nokdu 에서 실패한다(`1 failed, 101 passed, 5 skipped`).

- **분포 이동이 아니라 단독 이상치다.** 2위 sharosugil 23.00p 와 11.5%p 벌어져 있고,
  25~30p 구간 거점은 **0곳**이다. 임계값 문제로 보고 테스트를 늦추면 안 된다.
- **변경 전에도 최대 격차였다** — 종전 대표 공실률 34.2% 기준 격차 ≈28.6p 로 임계
  바로 아래였고, 이번 재산출(34.2 → 40.3%)이 넘긴 것이다. 새로 생긴 이상이 아니라
  **가려져 있던 이상이 드러난 것**으로 읽어야 한다.
- 단서: nokdu 는 **층 미상 점포 비율 55%** 로 인접 거점 중 최고다(sillim 49% ·
  sharosugil 45%). 층별개요 스냅샷 자체는 정상이다(2026-08-15 390지번, 부분수집 아님).
  앵커 5.63% 는 sillim 과 **같은 R-ONE 매핑**인데 sillim 추정은 25.3% 라, 같은 앵커에
  두 배 가까운 추정이 붙는다 — nokdu 쪽 분모·분자 귀속을 따로 봐야 한다.
- 조치 전까지 nokdu 수치는 **신뢰하지 말 것**. 테스트는 실패인 채로 둔다(가리지 않는다).

**미착수(계획 §3 대비)**: 히스토리 타임라인(백엔드 `/history` 는 있으나 프론트 미연결) ·
상세 차트(d3·plotly 설치돼 있으나 import 0건) · 유동/밀도 히트맵 레이어(샘플 상수).
glTF 실측 모델은 자산 0개이며 `/buildings/{id}/model` 은 스텁이다.

### 유동·밀도 레이어 — 배선 완료 (2026-08-23)

배선하려다 재료부터 확인했고, **경로가 정해졌다.** 다음 세션이 다시 조사하지 않도록 남긴다.

- ~~⚠ 시간 슬라이더가 아무 데도 연결돼 있지 않다~~ ✅ **해소(08-23).** `hour` 가
  `/heatmap/footfall` 질의에 들어가고 effect 의존성에도 올라갔다. 예전에는 `hour` 가
  오버레이 effect 의 의존성 배열에 없어 **드래그해도 지도가 안 바뀌었다** — 데이터와
  별개인 독립 결함이었다.
- ~~기존 TRDAR `seoul_trdar_flpop` 은 상권 단위 총량이고 **좌표가 없다**~~
  🔴 **틀렸다(08-23 배선하며 확인).** 수집기 원본에는 없지만 GNN 피처로 만든
  `gold/features/trdar_demand.parquet` 에 `trdar_lat`·`trdar_lon`·`trdar_area_m2` 가
  **190상권 전부 결측 0** 으로 들어 있다. 이미 Program 이 이 파일을 쓰고 있었다(§0-C).
  "재료가 없다"가 아니라 **저장소 안의 재료를 못 찾고 있었다** — 그래서 아래를 오늘 배선했다.
  (시간대가 6구간뿐이라는 지적은 맞다. 그건 아래 한계로 남는다.)
- `data/collectors/living_population.py` 는 **자치구 단위 0~100 정규화**라 격자가 아니다.
  이름 때문에 이게 격자 소스로 오해되기 쉬운데 아니다(SCORE_BANDS 용).
- ✅ **쓸 수 있는 것을 찾았다**: 서울 열린데이터 `SPOP_LOCAL_RESD_DONG` 이
  **행정동 × 시간대(00~23) 생활인구 실측**을 준다(63만 행, 08-23 응답 확인).
  `SEOUL_OPENAPI_KEY` 는 이미 `data/.env` 에 채워져 있다 — **막는 것이 없다.**
  거점↔행정동 매핑은 저장소에 없지만 카카오 `coord2regioncode`(키 보유)로 54거점
  역지오코딩하면 된다. 카카오의 10자리 행정동 코드 앞 8자리가 `ADSTRD_CODE_SE` 와 맞는다.

#### 오늘 배선한 것 (TRDAR 상권)

| | 내용 |
|---|---|
| 산출물 | `data/pipelines/build_page_footfall.py` → `gold/platform_page_footfall.json`(81KB, 54거점 190상권) |
| 서빙 | `services/footfall_layer.py` — 셀마다 **최근접 상권** 값을 얹는다(`rent_layer` 와 같은 방식) |
| API | `GET /heatmap/footfall?district=..&hour=0~23` · `GET /heatmap/density?district=..&metric=flpop\|stor` |
| 화면 | `MapShell` — 슬라이더가 실제 질의를 바꾼다. 범례에 `TRDAR 상권단위` 배지 |

**파케이를 런타임에 들이지 않는다.** `_district_context` 가 `import pandas` 로 시작해
**배포에서 한 번도 돈 적이 없던** 사고(feature-program §0-5)와 같은 조합이다. 분석은
파케이, 서빙은 JSON — 같은 파일에서 파생시키고 생성 경로는 하나다. 추적 예외도 걸었다.

검증: `tests/test_footfall_layer.py` 13건. 그중 둘이 이 변경의 요점을 붙든다 —
`test_hour_changes_the_values`(슬라이더가 값을 바꾸는가)와
`test_missing_artifact_disables_layer`(없을 때 0 으로 채우지 않는가).

#### 남는 한계 — 숨기지 않는다

- ~~**시간 해상도 6구간.**~~ ✅ **해소(08-24).** 생활인구(행정동 × 24시간)로 시간 축을
  갈아끼웠다 — 아래 "다음 단계" 로 적어 둔 설계를 그대로 구현했다. 슬라이더 24개
  상태가 모두 다른 값을 낸다(`test_all_24_hours_are_distinguishable`).
  산출물이 그 거점을 담지 못하면 응답이 `time_source: "trdar_band"` 로 **6구간임을
  밝히고** 물러난다 — 조용히 섞이지 않는다. 평일/주말도 갈린다(`daytype`).
  ⚠ 두 눈금은 셀 절대값 스케일이 약 4배 다르다(`share_basis`: `hour24` vs `band6`).
  프론트가 응답 min/max 로 재정규화하므로 색상은 같지만 **두 응답의 v 를 직접
  비교하면 안 된다.**
- **공간 해상도는 상권 단위.** 거점당 상권이 1~9곳(중앙 3)이라 격자 셀들이 같은 값을
  공유한다. 예전 샘플보다 거칠어 보이는데, **매끄러움은 데이터가 아니라 난수가**
  만들던 것이었다. 응답의 `resolution: "trdar"` 와 범례 배지가 이 해상도를 밝힌다.
- **'인구밀도'가 아니라 '유동인구 밀도'다.** 상주인구가 아니므로 라벨을 바꿨다.

#### ✅ 구현됨(08-24) — 아래 생활인구 설계를 그대로 배선했다

TRDAR 과 생활인구는 어느 하나가 낫다기보다 **축이 반대**다:

| | TRDAR(오늘 배선) | 생활인구(계획) |
|---|---|---|
| 시간 | 6구간 | **00~23시 실측** |
| 공간 | 상권(거점당 1~9) | 행정동 — **거점보다 넓다** |

즉 시간은 생활인구가, 공간은 TRDAR 이 낫다. 합치는 것이 맞다 — 생활인구로 **시간
곡선**을 얻고 TRDAR 상권으로 **공간 가중**을 주면 두 축 모두 실측이 된다.

**이대로 배선했다(2026-08-24).** 셀 값 = (최근접 상권 유동총량) × (그 시각의 생활인구
구성비). 절대 인원을 그대로 쓰지 않은 이유가 중요하다 — 행정동 값(수만 명)을 셀에
얹으면 상권 값을 덮어써서 **거점 안의 공간 차이가 사라진다.** 시간 축을 얻고 공간을
잃는 거래가 된다. 그래서 구성비만 갈아끼웠다.

구현: `data/collectors/living_population_hourly.py`(수집) →
`data/pipelines/build_page_footfall_hourly.py`(Gold) →
`services/footfall_layer.hourly_share()`(서빙). 체인 러너는
`scripts/run_page_hourly_chain.py`.

전제였던 `silver/hub_adong.json`(거점↔행정동)이 **없어서** 생활이동 Gold 가
`districts: {}` 빈 껍데기였다 — `build_hub_adong` 을 돌리는 것이 실제 병목이었다.

⚠ 프로브로 확정한 함정 하나: 카카오 `coord2regioncode` 는 **10자리** 행정동 코드를,
서울 생활인구 `ADSTRD_CODE_SE` 는 **8자리**를 쓴다(`code10[:8]` 로 일치, 424개 전량
대조). 정규화가 없으면 필터가 한 행도 못 맞히면서 **예외도 안 난다** — 날짜마다
"0행 저장" 으로 조용히 끝난다.

**정해진 설계**(생활인구) — 두 축의 출처를 나눠 적는다:

| 축 | 소스 | 성격 |
|---|---|---|
| 시간 | 서울 생활인구 행정동×00~23시 | **실측** |
| 공간 | 건물 좌표 × 상업면적 가중(`page_building_master.geojson`) | **실측 앵커 + 분포는 모델** |

⚠ 행정동은 거점보다 넓다. 그래서 이 배선으로도 **거점 내 공간 분포는 여전히 모델**이다
— 화면 라벨을 "생활인구 실측 격자"라고 적으면 안 된다. 50m 격자 실측은 REST 가 아니라
파일 다운로드라 별도 과제다.

남은 신설: `collectors/living_pop_dong.py` · 거점↔행정동 매핑.
(`api/v1/heatmap.py::footfall` 과 `MapShell` 슬라이더는 08-23 에 섰다 — 생활인구를
붙일 때는 `footfall_layer` 의 시간 축만 갈아끼우면 된다.)

⚠ 아래 **§3·§4 는 착수 시점의 계획**이라 Mapbox 와 3D 트윈을 전제한다 — 이력으로만 읽을 것.
실제 베이스맵은 네이버 지도(`lib/naverMap.ts`)이고 `mapbox-gl` 은 2026-08-25 에
의존성에서 제거됐다(`docs/decision-infra-layer-2026-08-25.md`). §2 의 실행 절차는
2026-09-04 에 현재 값으로 고쳤다. **§3 의 4번 항목(`BuildingTwin` · @react-three/fiber ·
GLTFLoader · Mapbox custom layer)은 통째로 무효다** — 2026-09-05 에 폐기하고
`BuildingViewer`(2D 층 스택 + 거리뷰)로 갈았다(§0 머리말 · feature-posting.md §0-V).
2·3번의 `DistrictMap.tsx`·`VacancyHeatmap.tsx` 도 그 이름으로 만들어지지 않았다 —
`MapShell.tsx` 한 곳에 들어 있다.

### 분모에서 고시원·독서실·주거를 걷어냈다 (2026-08-23)

`test_gold_anchor_comparison_attached` 가 nokdu 앵커 격차 **+34.55%p** 로 상한 30 을 넘겨
계속 빨간 상태였다. 원인은 앵커가 아니라 **분모**였다.

#### 진단 — 1층은 만실인데 상층부만 무너진다

| 층 | nokdu | sillim | garosugil |
|---|---|---|---|
| 1층 | 0.0% | 0.3% | 0.6% |
| 2층 | **49.5%** | 19.8% | 11.7% |
| 3층 | **74.0%** | 33.1% | 23.2% |
| 5층 | **90.2%** | 60.6% | 43.7% |

nokdu 는 **고시촌**이다. 2층 이상 분모 903행 중 독서실 269 · 고시원 137 · 다중주택 66 ·
다가구주택 46 = **60%** 였다. 이들은 용도상 근린생활시설이라 `NON_CAPACITY_PURPS`
negative 필터를 통과해 분모에 들어오는데, **분자(상가정보)는 이들을 점포로 싣지 않는다.**
숙박(호텔 305실 = 점포 1건)·지식산업센터("기타공장")를 걷어냈던 것과 같은 종류의 오염이다.

앵커 탓이 아니라는 근거: 같은 신림역 앵커(5.63%)를 쓰는 sillim 은 +19.63%p 였다.

#### 판단 기준 — "분자가 그 용도를 볼 수 있는가"

용도별로 **지상 2층↑ 상업층 중 점포·인허가가 확인된 층의 비율**을 재서 갈랐다
(54거점 전수, `store_flr_nos ∪ lic_flr_nos`).

| 남긴 용도 | 확인율 | 걷어낸 용도 | 확인율 |
|---|---|---|---|
| 기타판매시설 | 58.3% | 다가구주택 | 5.5% |
| 일반음식점 | 58.1% | 고시원 | 11.0% |
| 미용원 | 56.0% | 독서실 | 22.4% |
| 휴게음식점 | 52.8% | 다중생활시설 | 23.6% |
| 의원 | 51.3% | | |
| **학원** | **47.6%** | | |
| 소매점 | 38.1% | | |

두 무리가 확연히 갈린다(≥38% vs ≤24%). **학원은 47.6% 로 남긴 무리 한복판이라 유지한다** —
용도상 근생 임차 단위가 맞고 상가정보 "교육" 대분류로 분자에도 실린다. 주거류는 이
5종으로 전수 커버된다(57거점 bronze 점검: 추가 후 분모에 남는 주거 용도 0건).

#### 결과 — 54거점 전부 상한 안으로 들어왔다

```
python -m data.pipelines.build_building_attrs      # com_area_flr·com_flr_nos 가 필터를 탄다
python -m data.pipelines.recalc_floor_ouln         # API 콜 없이 bronze 재계산
python -m data.pipelines.build_page_master
python -m data.pipelines.calibrate_vacancy
```

| | before | after |
|---|---|---|
| nokdu 격차 | +34.55%p | **+24.16%p** |
| sharosugil | +23.00%p | +12.07%p |
| sillim | +19.63%p | +12.89%p |
| 평균 \|격차\| | 9.87%p | **7.67%p** |
| 중앙 \|격차\| | 9.27%p | **7.28%p** |
| 상한(-20<gap<30) 위반 | nokdu 1건 | **0건** |
| 집계 건물 수 | 22,055 | 22,056 |
| capacity 합 | 66,468 | 63,565 (-4.4%) |

건물 수가 그대로인 채 capacity 만 줄었다 — 건물을 뺀 것이 아니라 **그 건물의 빈 층을
분모에서 뺀 것**이 맞다. `apps/backend` 전체 169 passed / 5 skipped.

#### 남은 판단 5건 — 전부 결말(2026-08-23 저녁)

아래 다섯은 같은 날 저녁 인허가 27종 **실수집**과 함께 정리했다. 결과는 다음 절에 있다.

1. **`build_vacant_units` 재실행** — 했다. 유닛이 **580 → 216**(-63%)으로 줄었다.
   면적(-4.8%)이 아니라 **분자**가 이유다: 인허가 27종이 영업을 더 확인하면서
   `empty`/`high` 로 잡히던 건물이 실제로는 영업 중으로 판명됐다. 종전 580유닛 목록은
   그만큼 **분자 결손으로 부풀어 있었다**.
2. **호스텔** — 뺐다(`NON_CAPACITY_PURPS`). 876층(1층 71 · 2층↑ 805, 확인율 37.0%).
   같은 숙박 계열(관광호텔 6.5% · 여관 3.6% · 호텔 7.5%)은 07-28 에 이미 뺐고
   필터 문자열에 "호스텔"이 없어 이것만 남아 있었다.
3. **금융업소** — 뺐다. 871층(1층 282 · 2층↑ 589, 확인율 22.9%). 은행 지점은
   **상가업소정보에 대분류 자체가 없고**(garosugil 5,264행 전수: 금융 0건) 지방행정
   인허가에도 없다. 분자가 구조적으로 못 보는 용도다.
   → 2·3이 안전한 이유: 분모는 `capacity_floors()` 에서 **층별개요 상업층 ∪ 점포·인허가
   확인층**이라 용도를 빼도 근거 있는 층은 도로 들어온다. 실제로 빠지는 것은 분자가
   아무것도 못 본 층뿐이다(호스텔 546 · 금융업소 568).
4. **`recalc_capacity`(전유부)** — 실행했다. 54거점 완주, 실패 0(거점 단위 루프로 돌려
   185MB 대장에서도 메모리가 버텼다). 예: garosugil 집합 capacity 3,867 → 3,852.
   집합건물은 집계에서 빠지므로 앵커에는 영향이 없고 상수-산출물 불일치만 지웠다.
5. **nokdu 잔여** — 문서가 남겨 뒀던 "집계 건물이 소형이라 앵커 모집단이 어긋난다"는
   **틀렸다.** nokdu 집계 건물 395동 중 373동(94%)이 R-ONE 중대형 기준(3층↑ 또는
   330㎡ 초과)을 충족하고 지상층 중앙값이 5다. 원인은 **커버리지**다 — 2층↑ 상업층
   확인율이 nokdu 26.8% 로 54거점 최저다(garosugil 52.4% · sillim 57.8%).
   27종 반영 후에도 최대 격차(+21.77%p)는 여전히 nokdu다.

### 인허가 27종 확장 · 생활이동 골격 (2026-08-23)

#### 왜 — 분모를 고친 다음의 병목은 분자다

앞 절에서 고시원·독서실을 분모에서 걷어내 평균 격차를 9.87→7.67%p 로 줄였다. 남은
과대추정은 **분자가 못 보는 층**에서 온다. 54거점 지상 2층↑ 상업층 55,286행을 용도별로
"점포·인허가가 확인된 층인가"로 재 보면:

| 용도 | 층수 | 확인율 | 당시 인허가 |
|---|---|---|---|
| 일반음식점 | 9,094 | 58.1% | ✅ |
| 의원+한의원+치과의원 | 8,778 | 49~67% | ❌ |
| 소매점+상점 | 7,877 | 34~38% | ❌ |
| 학원+교습소+교육원 | 7,167 | 17~57% | ❌ |
| 미용원+이(미)용원+목욕장+안마 | 1,794 | 13~56% | ❌ |
| 당구장+체력단련장+탁구장+체육도장+골프 | 1,168 | 29~55% | ❌ |
| 노래연습장+PC방+게임 | 480 | 35~64% | ❌ |
| 유흥주점+기타위락 | 466 | 52~53% | ❌ |

**미커버 용도가 분모의 69.7%**, 그 중 확인 안 된 층이 약 21,300개다. 인허가는 5종만
받고 있었다(`seoul_licensing.SERVICES`).

#### 서비스 ID 를 추측하지 않고 전수로 찾았다

서울 열린데이터광장 서비스명은 `LOCALDATA_{CCGGSS}` 이고 없는 조합은 ERROR-500 을
낸다. 대분류 01~12 × 그룹 × 세부를 찔러 **살아 있는 161종**을 뽑고, 응답의 업소명
표본으로 업종을 특정했다(예: `LOCALDATA_114302` → GS25·세븐일레븐·성심수퍼 → 담배소매업).
CC=13,14 는 비어 있어 코드 공간이 12 에서 끝나는 것도 확인했다.

**5종 → 27종.** 서울 전체 1,250,310행, 페이징 약 1,277콜(`SEOUL_OPENAPI_KEY`, 쿼터 관대).
전 27종 응답 확인 완료.

#### 못 채운 것 — 학원이 여기 없다

**학원·교습소(7,167층)는 지방행정 인허가에 없다.** 학원법상 교육청 소관이라 LOCALDATA
계열이 아니고, 01~12 전수 스캔에서 나오지 않았다. 서울시 교육청 '학원·교습소 현황'을
따로 받아야 한다 — 1순위로 꼽았던 항목이라 별도 소스로 옮겨 단다.
탁구장업(121층)은 체육시설업 그룹에서 표본이 비어 특정하지 못했다.
통신판매업(940,503행)은 **의도적으로 뺐다** — 온라인 사업자라 신고 주소가 자택·사무실이고,
층 확인에 쓰면 없는 점포를 만든다. 대규모점포도 뺐다(집합건물이라 집계 제외 + 확인율 98.3%).

#### 곁가지 소득 — 인허가에 `SITEAREA`(영업장 면적)가 있다

일반음식점 29.70㎡ · 미용업 177.80㎡ 처럼 **업소별 실측 면적**이 들어 있다. Posting 의
`avg_store_pyeong()`(A)는 지금 KOSIS 임차료에서 역산한 대리값이고 그것이 이 모델의
유일한 가정인데, 이 필드는 **서울 전수·업태별 실측**이다. 공정위 정보공개서
(`ESSENTIAL_PARAMETER_ERROR` 로 막힌 §0-E)를 기다릴 필요가 없어질 수 있다.
→ 수집 후 업태별 SITEAREA 중앙값을 A 후보로 대조할 것.

#### 생활이동 파이프라인 골격

「수도권 생활이동」(서울시×KT, OA-22300 계열)은 **도착시간(0~23) · 출발/도착 행정동 ·
성별 · 나이 · 이동유형(출근·등교·쇼핑·관광·병원·귀가·기타) · 평균 이동시간 · 이동인구**를
준다. 행안부 「지역별 인구이동 현황」과는 다른 물건이다 — 그쪽은 주민등록 전입신고라
**월 단위 거주지 이전**이고 시간대 축이 없어 상권 방문을 못 센다.

```
data/pipelines/build_hub_adong.py       거점 ↔ 행정동 + 상업연면적 가중 (카카오 coord2regioncode)
data/collectors/living_migration.py     ZIP/CSV 스트리밍 → 거점 도착 행정동만 집계 → bronze
data/pipelines/build_page_migration.py  bronze → gold/platform_page_migration.json
```

설계에서 갈린 세 지점:

1. **파일이지 REST 가 아니다.** `/F/` 데이터셋이고 일별 ZIP 이다. 행정동 OD × 24시간 ×
   성 × 연령이라 하루치도 수백만 행 — 원본을 bronze 에 쌓지 않고 **스트리밍으로 읽으며
   즉시 접는다**(`build_building_attrs` 가 85MB 대장을 행 단위로 접는 것과 같은 이유).
   다운로드는 포털 로그인 뒤라 자동화하지 않고, 내려받은 디렉터리를 인자로 받는다.
2. **행정동↔거점을 좌표로 잇는다.** `stores_raw.lnoAdr` 에서 나오는 것은 법정동인데
   생활이동은 행정동이고 둘은 1:1 이 아니다. 변환표를 들이면 그 표가 낡으므로 건물
   좌표를 카카오 coord2regioncode 에 넣어 직접 받는다(약 100m 격자로 뭉쳐 호출 절약).
   한 거점이 행정동 여럿에 걸리므로 **상업 연면적(`com_area_flr`) 가중**을 함께 낸다 —
   08-23 분모 정정이 반영된 값을 그대로 탄다.
3. **컬럼명을 고정하지 않는다.** 포털 배포분마다 헤더 표기가 흔들려, 정규화 후 부분일치로
   매핑하고 하나라도 못 찾으면 **실제 헤더를 찍고 멈춘다.** 조용히 기본값을 쓰면 근거 없는
   숫자가 gold 까지 간다. 이동인구가 `*`(KT 비식별)인 행도 0 이 아니라 `masked_rows` 로
   따로 센다.

산출물은 `build_page_footfall`(체류·밀도)과 짝이다 — 그쪽이 "얼마나 있나", 이쪽이
"몇 시에 · 누가 · 어디서 왔나"다. 둘 다 `resolution` 을 응답에 실어 **행정동/상권 집계이지
건물 단위 실측이 아님**을 밝힌다.

실행 순서:
```
python -m data.pipelines.build_hub_adong                      # 선행 (카카오 키 필요)
python -m data.collectors.living_migration <내려받은디렉터리>
python -m data.pipelines.build_page_migration
```

### 인허가 27종 실수집 — 앵커 격차 7.67 → 6.36%p (2026-08-23 저녁)

앞 절이 **설계**였다면 이 절은 **실측**이다. 서울 전체 1,250,310행을 27종으로 훑어
(1,277콜 · 40분) 54거점 bronze 를 다시 깔았다. 거점당 인허가가 5~6배로 늘었다
(garosugil 8,257행 → 45,946행 · 701MB).

#### 수집기를 세 군데 고쳤다 — 돌리기 전에

1. **`SITEAREA`(영업장 면적)를 버리고 있었다.** `_KEEP` 에 없어서, 앞 절이 "수집 후
   업태별 중앙값을 Posting 의 `avg_store_pyeong`(A) 대조에 쓰라"고 적어 둔 필드가 그대로
   사라질 참이었다. 27종 중 11종(식품접객·공중위생·주점·병원)에 실측값이 들어 있다.
2. **업종별 중간 저장이 없었다.** 1,277콜을 무저장으로 돌리는 구조라 첫 시도에서
   일반음식점 537콜을 다 스캔한 뒤 세션이 끊기며 **통째로 잃었다**(`floor_capacity` 가
   150동마다 저장하게 된 것과 같은 사고다). 업종 하나가 끝날 때마다
   `bronze/_licensing_stage/{업종}.json` 에 떨어뜨리고, 재실행하면 오늘 자 스테이지가
   있는 업종은 콜 없이 건너뛴다. **미완주 업종이 하나라도 있으면 bronze 병합을 하지
   않는다** — 반쪽짜리 27종이 5종짜리 기존 판을 덮어 최신본이 되는 것이 더 나쁘다.
   병합도 업종 하나씩만 메모리에 올린다(일반음식점 한 종의 거점 적재가 570,392행).
3. **같은 점포가 업종 수만큼 세어졌다.** 편의점 한 곳이 담배소매업 + 안전상비의약품판매
   + 휴게음식점으로 3장을 갖는다. 3거점 실측 중복률이 5종 0.6% → 27종 **3.8%**.
   층은 합집합이라 무해하지만 `lic_unknown`(층 미상)은 상한 산정에서 빈 층을 채우므로
   공실이 과소추정된다. `_licensed_pip` 에서 (업소명, 지번주소)로 dedupe 한다.

#### 결과 — 53거점 개선 · 악화 0거점

```
python -m data.collectors.seoul_licensing --force
python -m data.pipelines.build_building_attrs
python -m data.pipelines.recalc_capacity        # 전유부(거점 루프)
python -m data.pipelines.recalc_floor_ouln
python -m data.pipelines.build_page_master
python -m data.pipelines.calibrate_vacancy
python -m data.pipelines.build_vacant_units
```

| | before | after |
|---|---|---|
| 평균 \|격차\| | 7.67%p | **6.36%p** |
| 중앙 \|격차\| | 7.28%p | **6.42%p** |
| 최대 | nokdu +24.16 | **nokdu +21.77** |
| 최소 | cheongdam -7.45 | **cheongdam -10.66** |
| 상한(-20<gap<30) 위반 | 0건 | **0건** |
| capacity 합 | 63,565 | 63,692 (**+127**) |
| active 합 | 52,929 | **54,564 (+1,635)** |
| 집계 건물 | 22,056 | 22,074 |

**분모가 아니라 분자가 움직였다.** 호스텔·금융업소를 뺐는데도 capacity 가 오히려
+127 인데, 분모가 "층별개요 상업층 ∪ 점포·인허가 확인층"이라 새 인허가가 확인한 층이
분모에도 함께 들어오기 때문이다. 개선은 전적으로 active +1,635(+3.1%)에서 왔다.

#### 인허가 건수는 1.5~1.7배인데 확인율은 +3%p — 왜

6거점 실측(지상 2층↑ 상업층 중 점포·인허가로 확인된 층):

| 거점 | 확인율 | 인허가 귀속 |
|---|---|---|
| nokdu | 24.6% → **26.8%** | 274 → 391 |
| gwangjang | 29.4% → **30.8%** | 726 → 1,127 |
| ikseon | 40.9% → **42.7%** | 1,139 → 1,513 |
| garosugil | 48.9% → **52.4%** | 946 → 1,503 |
| sillim | 51.3% → **57.8%** | 1,070 → 1,857 |
| 6거점 계 | 42.1% → **45.0%** | 인허가 전용층 289 → **435** |

새로 받은 27종의 상당수가 **이미 상가정보가 보던 층**에 겹친다. 순증은 "인허가로만
확인된 층" 289 → 435(+50%)뿐이다. 남은 미확인층의 병목은 업종 종류가 아니라
**층 표기 자체가 없는 것**일 가능성이 크다 — 다음 진단 대상이다.

#### 새로 드러난 것

- **garak 만 인허가가 안 붙는다.** 좌표 자가보정이 남북 **-175.0m** 로 어긋나(다른
  53거점은 -0.2 ~ -1.2m) 영업 4,891건 중 12건만 2동에 귀속됐다. 그래서 garak 은
  이번 변경으로 격차가 **0.00%p** 움직였다(유일). 오늘 생긴 회귀가 아니라 드러난 것이다.
- **cheongdam 이 -10.66%p 로 하한 쪽 최대**가 됐다(before -6.07). 분자 확장이 일부
  거점을 앵커 아래로 밀고 있다. 아직 가드레일(-20) 안이지만 계속 넓히면 부딪힌다.

#### 곁가지 소득이 실제로 들어왔다 — `SITEAREA` 로 Posting 의 A 를 대조했다

앞 절이 예고한 대로 인허가에 **업소별 실측 영업장 면적**이 들어 있다. 5거점 영업 중
표본으로 업태별 중앙값을 냈다(5~2,000㎡ 로 자름):

| 업태 | 표본 | 중앙 ㎡ | 중앙 평 |
|---|---|---|---|
| 일반음식점 | 18,446 | 62.7 | **19.0** |
| 미용업 | 4,594 | 44.0 | 13.3 |
| 휴게음식점 | 4,444 | 33.4 | 10.1 |
| 제과점영업 | 627 | 43.4 | 13.1 |
| 즉석판매제조가공업 | 1,367 | 21.0 | 6.4 |
| 체력단련장업 | 494 | 203.2 | 61.5 |

현재 `posting_revenue.avg_store_pyeong()` 는 KOSIS 임차료 역산으로 **premium 12.6평 ·
value 7.1 · factory 7.1** 을 쓴다. 실측과 나란히 놓으면 **일반음식점 19.0평 vs
premium 12.6평** — 대리값이 1.5배 작다. 매출이 면적에 비례하는 모델이라 이 차이는
그대로 매출·마진으로 간다.

⚠ 다만 두 값의 **모집단이 다르다**(KOSIS 전국 업태 평균 vs 서울 인허가 전수) 이 절에서는
대조까지만 하고 모델은 건드리지 않았다. A 를 실측으로 갈아끼우는 것은 tier↔업태 매핑을
정한 뒤의 별도 판단이다 — 공정재 정보공개서(`ESSENTIAL_PARAMETER_ERROR` 로 막힌 §0-E)를
기다릴 이유는 없어졌다.

### no-com-floor — 커버리지가 세고 있던 것은 결손이 아니었다 (2026-09-04)

`대표 집계 커버리지` 가 65/66 에서 멈춰 있었고 미달은 doksan 83.5% 한 곳이었다.
남은 13동을 수집 과제로 적기 전에 프리플라이트로 세어 보니 **회수 가능분이 0** 이었다:
13동 전부 2026-09-01 에 층별개요를 정상 수신했고, 지상 상업층이 0 으로 확정된
건물이다(`bronze/doksan/2026-09-01/bldg_flr_raw.json` 대조, 미시도 0동).

즉 커버리지가 재고 있던 것은 "덜 모았다"가 아니라 **라벨이 겹친다** 였다.
`floor_approx` 하나가 두 상태를 가리키고 있었다.

| 상태 | 뜻 | 옳은 대응 |
|---|---|---|
| 아직 못 쟀다 | 층별개요 미시도 | 수집한다 — 커버리지 분모에 남아야 한다 |
| 재 봤더니 상가층이 없다 | 층별개요 수신 · 지상 상업층 0 | **판정**이다 — 분모에서 빠져야 한다 |

둘째 것을 `no_com_floor` 로 갈랐다(`data/collectors/building_vacancy.NO_COM_FLOOR`).
강등은 API 콜 없이 `recalc_floor_ouln` 이 bronze 원본에서 한다.

**왜 지도에서도 빼는가.** `floor_approx` 의 capacity 는 *지상 전체 층수* 다. 그래서
13층 오피스텔(어반팰리스, 점포 2건)이 `capacity 13 / active 2` 로 계산돼 지도에
**"공실 84.6%"** 로 칠해지고 있었다. 강등 대상 634동의 지상층 주용도를 세면
여관 1,200 · 기타일반숙박 496 · 사무소 455 · 오피스텔 338 · 관광호텔 299 · 고시원 163
— `NON_CAPACITY_PURPS` 모집단 그 자체다. 상가 건물이 아닌 것에 상가 공실률을 붙여
그리고 있었던 셈이다.

**이 변경이 움직이지 않은 것.** 대표 공실률(`reference_vacancy_pct`)은 **66거점 전부
불변**이다 — 그 값은 원래부터 `expos_units + floor_ouln` 만 세었고, 강등분은 그 밖에
있었다. `calibration.json` 의 `rone_aligned.mid`(앵커 대조 대표값)도 66거점 불변이다.
움직인 것은 분모의 정의뿐이다. 지표를 올리려고 데이터를 뺀 것이 아니라는 근거가 이것이다.

**결과**

| | 전 | 후 |
|---|---|---|
| 커버리지 ≥90% 거점 | 65/66 (최저 doksan 83.5%) | **66/66 (전부 100%)** |
| 지도 표시 건물 | — | −672동 (상가 아님이 확정된 것) |
| `excluded_unknown` | 12,461 중 11,789 이 실은 판정분 | 판정분을 `excluded_determined` 로 분리 |

`excluded_determined` 는 coverage.json 에 새로 실리는 칸이다. **이 값이 커지는 것은
수집이 덜 됐다는 뜻이 아니다** — 그 구분이 이 절의 요점이다.

⚠ 강등은 **편도가 아니다.** `_TARGET_METHODS` 에 `no_com_floor` 가 들어 있어,
`NON_CAPACITY_PURPS` 나 `capacity_floors` 가 넓어지면 같은 bronze 원본에서 다시
`floor_ouln` 으로 올라온다. 회귀는 `data/tests/test_no_com_floor.py` (4건)가 고정한다.

## 1. 담당 코드 영역

```
apps/frontend/src/components/        3D 맵, 히트맵 레이어, 히스토리 타임라인
apps/frontend/src/pages/             상권/건물 상세 페이지
apps/frontend/src/lib/api.ts         백엔드 호출 (getBuildingHistory 등)
apps/backend/app/api/v1/buildings.py 건물 히스토리/3D 모델 경로
apps/backend/app/api/v1/districts.py 상권 정보 + /heatmap GeoJSON
```

## 2. Claude Code 설치/환경

```bash
cd apps/frontend
npm install                 # react, d3, plotly (three 계열은 2026-09-05 제거됨)
npm run dev                 # http://localhost:5173

# 네이버 지도 키 (.env) — NCP 콘솔 Web 서비스 URL 에 http://localhost:5173 등록 필수
echo "VITE_NAVER_MAPS_KEY_ID=xxxx" > .env
```

> 백엔드를 함께 띄워야 `/api` 프록시가 동작한다: `cd apps/backend && uvicorn app.main:app --reload`

## 3. 작성해야 할 코드 (순서)

1. **히트맵 API** (`apps/backend/app/api/v1/districts.py`) — `/heatmap` 스텁을 Gold 레이어 공실 데이터 기반 GeoJSON FeatureCollection으로 교체. 각 Feature에 공실률·예측값(Platform 연동) 속성 부여.
2. **Mapbox 베이스맵 컴포넌트** (`src/components/DistrictMap.tsx`) — Mapbox GL 지도 초기화, 거점 상권 좌표로 카메라 이동.
3. **히트맵 레이어** (`src/components/VacancyHeatmap.tsx`) — `/api/v1/heatmap/{id}` GeoJSON을 fill-extrusion 또는 heatmap 레이어로 렌더. 공실률에 따라 색상 매핑.
4. **3D 건물 트윈** (`src/components/BuildingTwin.tsx`) — @react-three/fiber + GLTFLoader로 `/buildings/{id}/model` glTF 로드. Mapbox custom layer로 좌표계 정합. **로딩 <3초 목표** (모델 LOD·캐싱).
5. **공실 히스토리 타임라인** (`src/components/HistoryTimeline.tsx`) — `getBuildingHistory`로 업종 변천사를 타임라인 UI로 표시. 폐업 사유 AI 요약(Program/LLM) 노출.
6. **상세 차트** — D3/Plotly로 공실 추이·예측 그래프.

## 4. Claude Code 작업 예시

```
/clear
/backend-dev 상권 공실 히트맵 GeoJSON 엔드포인트.
  districts.py 의 /heatmap 스텁을 Gold 레이어 기반 FeatureCollection 으로 교체,
  각 Feature properties 에 vacancy_rate 와 predicted_rate 포함.

/frontend-dev Mapbox 기반 DistrictMap + VacancyHeatmap 컴포넌트.
  /api/v1/heatmap 데이터를 받아 공실률 색상 히트맵으로 렌더,
  src/lib/api.ts 에 getHeatmap 추가.
```

## 5. 검증

- `cd apps/frontend && npm run build` — 타입체크 통과
- 브라우저에서 히트맵 색상·3D 모델 렌더 확인, **로딩 3초 이내**
- `cd apps/backend && pytest` — heatmap/buildings 응답 스키마(GeoJSON 유효성)
- 네트워크 탭에서 API p95 <200ms 확인
