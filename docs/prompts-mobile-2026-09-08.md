# 폰으로 추가로 할 수 있는 작업 — 프롬프트 (2026-09-08)

> 09-06 판 [prompts-mobile-sprint-2026-09.md](prompts-mobile-sprint-2026-09.md) 의 **추가본**이다.
> 그 14개 중 **12개가 소진**됐다(§0 대조표 — 문서가 아니라 파일로 셌다).
> 여기 있는 것은 **오늘 실측으로 비어 있음이 확인된 자리**뿐이다.
> 일반 규칙(두 경로·클라우드에서 불가능한 것): [../PlaceOS_Mobile_Dispatch_Prompts.md](../PlaceOS_Mobile_Dispatch_Prompts.md)

---

## 0. 먼저 — 옛 14개 중 무엇이 끝났나

`git log` 가 아니라 **산출물 파일**로 셌다(2026-09-08 실측).

| 옛 번호 | 상태 | 확인한 근거 |
|---|---|---|
| A1 D0 여정 지도 | ✅ | `docs/ux-persona-journey-2026-09-07.md` |
| A2 D1 기획안 .docx | ✅ | 커밋 28653a1 |
| A3 D2a 랜딩 PRD | ✅ | `docs/prd-landing.md` |
| A4-a 레퍼런스 해부 | ♻ 반복형 | 캡처가 쌓일 때마다 다시 쓴다 — 소진 개념이 없다 |
| A4-b 일러스트 | ✅ | `design/assets/illustrations/` |
| A5 도메인 | ⬜ **미착수** | `spaceos-twin.web.app` 이 문서 20곳에 그대로 |
| A6 M1 시나리오 | ✅ | `design/planning/01-scenario-m1-yeonnam.md` |
| B1 Tailwind 제거 | ✅ | `tailwind.config.ts` 없음 |
| B2 Pretendard·타입스케일 | ✅ | `public/fonts/` 에 subset woff2 3개(Regular·SemiBold·Bold) |
| B3 여백 토큰 치환 | 🔶 **부분** | `--sp-1`~`--sp-8` 섰다. 그러나 hex **558 → 388**(목표 100 이하) · var 202 → 543(채택률 **58.3%**, 목표 80%) |
| B4 4트랙 색 축 | ✅ | `--track-platform/page/posting/program` + pressed·soft |
| B5 컴포넌트 실투입 | ✅ | `@/design/components` import **7곳** |
| B6 글자 정리 | ❌ **역행** | PlatformConsole 805 → **989줄** · ProgramStudio 611 → **772줄** |
| B7 앵커 격차 probe | ✅ | `docs/finding-anchor-gap-2026-09.md` |
| B8 화면 루프 스크립트 | ✅ | `scripts/screen_loop.py` (실행 산출물 `reports/screen_loop.json` 은 아직 미커밋) |

**여기서 나온 오늘의 그림**: 토큰 층(D5-0~4)은 섰고, **화면 층(D5-6·D5-7)과 신뢰 문구(M2-2·M2-3)가 비었다.**
그리고 진행률 밖에서 **KPI② 를 직접 막는 자리 하나**가 여태 비어 있다 — 계정 화면(B9).

---

## 1. 오늘의 우선순위 한 줄

**B9 → B16 → B10 → B11** 순서다.
B9 는 KPI②(파일럿 5~10건)가 배선 100%인데 **파일럿 0건**인 이유의 절반이다 — 사람이 가입할 화면이 없다.
B16 은 그다음이다. 계획이 *"M2(신뢰)를 M3(화면)보다 먼저"* 라고 못박았고,
지금 프로덕션에는 **같은 거점의 공실률이 12.5% 와 20.8% 두 개가 같은 라벨로** 떠 있다.
B10·B11 은 디자이너 피드백 7개 중 **마지막 둘**이다.

---

# A군 — 폰만으로 완결 (Claude·GPT 모바일 앱)

## A7 · B2B 파일럿 아웃리치 — 퇴근길 30분

> **왜 지금**: `pppp_status.py` 가 네 트랙 100% 를 찍는데 그 밑에 이렇게 적혀 있다 —
> *"배선 100% 는 파일럿 0 건과 양립한다."* 계측은 `GET /api/v1/admin/usage` 로 이미 돌고,
> `active_orgs` 가 KPI② 에 대응한다. 지금 그 수를 올리는 건 코드가 아니라 사람 접촉이다.

```
너는 PlaceOS 창업자의 B2B 세일즈 파트너다. 파일럿 후보 접촉안을 만든다.

[제품] PlaceOS — 서울 66거점의 건물·층 단위 공실을 건축물대장으로 전수 실측하고,
그 위에 입점 회수기간(3-Tier)과 홍보 프로그램까지 잇는 디지털 트윈 SaaS.
[모델] DaaS 월 500만원 B2B 구독. 6개월차 파일럿 5~10건이 목표다.

[쓸 수 있는 실측 — 이 밖의 숫자를 만들지 마라]
 서울 66거점 전부 Tier1(건축물대장 실측)
 공실 유닛 인벤토리 66/66거점 664유닛 · 층 단위 공실 12,497개(confirmed 9,437 / probable 3,060)
 연남동 1,133동 공실 12.5%(R-ONE 앵커 5.0% · 격차 +7.5%p)
 신촌·이대 1,443동 17.2%(앵커 11.68%) · 홍대 1,325동 15.0%(앵커 8.1%)
 업종추천 Top-3 91.7%(거점 사전분포 89.3% 대비 Top-1 lift 5.3%)
 히트맵 4레이어(공실·임대·유동·밀도)가 같은 100m 격자

[대상 3군 — 군마다 따로 쓴다]
 ① 프랜차이즈 본사 출점팀   ② 자산운용사·리츠 리서치   ③ 자치구 상권활성화 담당

[군마다 낼 것]
 1. 그 사람이 지금 무엇으로 이 일을 하고 있는가 (대체재) — 한 줄
 2. 그 대체재 대비 우리가 주는 것 한 줄. **숫자로**
 3. 콜드메일 본문 120자 이내 + 제목 3안
 4. 파일럿 제안 1장 요약 — 무료 기간 / 그들이 내는 것 / 우리가 받는 것(피드백·사용로그)
 5. 첫 미팅 15분 대본 — 무엇을 화면으로 보여주고 무엇을 말로 하는가
 6. 거절 사유 3개와 그에 대한 한 줄 답

[금지]
 - 실측 밖 숫자. 필요하면 "[측정 필요]" 로 남겨라
 - "AI 기반"·"혁신적인"·"원스톱" 같은 내용 없는 수식어
 - 앵커 격차(+5.5~+7.5%p)를 숨기지 마라. A8 의 답변을 짧게 붙여라
 - 개인정보·연락처를 지어내지 마라. 대상은 "역할"로만 적는다
```

## A8 · 앵커 격차 1분 답변 — 점심 15분

> **왜 지금**: 미팅에서 반드시 나온다 — *"한국부동산원은 5%라는데요?"*
> 실측 판정은 이미 났다(`docs/finding-anchor-gap-2026-09.md`). 남은 건 **말로 바꾸는 것**이다.

```
아래 실측을 미팅에서 60초에 말할 수 있는 답변으로 바꾼다.

[상황] 투자자·프랜차이즈 본사가 묻는다. "한국부동산원 공실률은 5%인데 왜 12.5%죠?"

[실측]
 우리: 건축물대장 전수. 연남동 1,133동 대표공실 12.5%
 R-ONE: 중대형 상가 **표본** 조사. 연남동 앵커 5.0% → 격차 +7.5%p
 신촌·이대 17.2% vs 11.68% · 홍대 15.0% vs 8.1% — 3거점 전부 우리가 높다
 ⚠ 검증된 사실 (docs/finding-anchor-gap-2026-09.md) — 이걸 틀리게 말하면 안 된다:
   "R-ONE 대상 규모(중대형)로 좁히면 격차가 줄어든다"는 가설은 **기각됐다. 반대로 커진다.**
   중대형만 남기면 연남 공실률은 12.5% 가 아니라 **20.8%**, 격차는 +7.5%p 가 아니라 **+15.8%p** 다
   원인은 표본/전수 차이가 아니라 **큰 건물의 분자 결손**이다
   (상가정보 flrNo 공란 35.8% · 그 행의 hoNo 도 100% 공란이라 내부 복원 불가)
 즉 "우리는 전수라서 높다"는 설명은 **쓰면 안 된다**. 실측이 그 반대를 가리킨다

[낼 것]
 1. 60초 구두 답변 — 격차를 인정하고 시작한다. 변명이 아니라 정의 차이로 설명한다
 2. 30초 압축판 (엘리베이터용)
 3. 되묻는 질문 하나 — 상대가 어느 수를 필요로 하는지 가르는 질문
 4. 화면에 두 수를 같이 그린다면 라벨 문구 2개 (전수 / 앵커)
 5. 절대 하면 안 되는 답변 3개와 그 이유

[금지]
 - 격차를 작아 보이게 만들지 마라. "오차 범위"라고 부르지 마라
 - 기각된 가설을 근거로 쓰지 마라(위 ⚠)
 - "우리 데이터가 더 정확하다"는 단정 — 다른 것을 재는 두 수다
```

## A9 · D4 Lovable 랜딩 실행 — 주말 1시간

> **왜 지금**: PRD(`docs/prd-landing.md`)와 일러스트(`design/assets/illustrations/`)와
> 토큰(`design/tokens/tokens.json`)이 다 나왔다. Lovable 은 폰 브라우저에서 돈다.
> ⚠ **경계**: 생성까지가 폰이다. 코드를 내려받아 `apps/landing/` 으로 편입하는 것은 데스크톱이다.

```
[Lovable 에 붙여넣는다. 첨부: prd-landing.md · tokens.json · 일러스트 PNG/SVG · 기획안 docx]

첨부 자료로 PlaceOS 랜딩 페이지를 만든다.

[반드시 지킬 것]
 1. 색은 첨부한 tokens.json 값만 쓴다. 새 색을 만들지 마라
    주색 #0EA5B7 · 배경 #F4F7FB · 선 #1C2533
    #03C75A(네이버 그린)는 네이버 연동을 말하는 자리에만 — 남발 금지
 2. 지도·대시보드를 만들지 마라. 스크린샷이 들어갈 **빈 슬롯**만 둔다
 3. 섹션 순서·카피는 첨부 PRD 를 따른다. 새 섹션을 발명하지 마라
 4. 숫자는 PRD 에 있는 것만. 없는 수치를 채우지 마라
 5. 모바일 우선. 접히는 것은 PRD 가 지정한 것만 접는다

[출력] 단일 페이지. React + CSS 변수. 외부 UI 킷을 새로 끌어오지 마라
```

**받은 뒤 (데스크톱에서)**: 코드를 내려받아 `apps/landing/` 으로 편입한다 —
Lovable 에 두면 배포 라인이 둘로 갈린다(main 푸시 → Cloud Run 하나로 유지).

## A10 · M6 데모 대본 — 퇴근길 30분

```
5분 데모 대본을 쓴다. 라이브 데모가 무너지는 자리까지 같이 적는다.

[시나리오] 소상공인 김민수(34) · 카페 창업 · 예산 1.5억 · 연남동
 ① 연남동은 어떤 상권인가 → ② 어디가 비었나 → ③ 얼마고 몇 개월에 회수되나 → ④ 어떻게 알리나
[화면] 좌측 레일 6탭 — 서울 · 거점 · Platform · Page · Posting · Program
[실측] 연남동 1,133동 · 공실 12.5%(앵커 5.0%) · 공실 유닛 664(66거점) · 업종추천 Top-3 91.7%

[낼 것]
 1. 분 단위 대본 — 0:00~5:00. 각 구간에 「조작 / 화면에 뜨는 것 / 말할 문장」 3줄
 2. 각 구간의 **실패 신호**와 **폴백** — 예: 지도가 3초 안에 안 뜨면 무엇으로 넘어가나
 3. 미리 열어 둘 탭 목록 (라이브 로딩을 피할 자리)
 4. 데모 중 절대 클릭하면 안 되는 것과 그 이유
 5. 끝나고 남기는 한 문장 (상대가 기억할 문장)

[금지]
 - 없는 기능을 전제하지 마라. 위 6탭 안에서만 움직인다
 - "여기서 AI가 추천해줍니다" 같은 뭉뚱그림 — 어떤 값이 뜨는지 말한다
 - 앵커 격차 질문을 대본에서 빼지 마라. A8 의 60초 답변을 4:00 자리에 넣는다
```

## A11 · A5 도메인 (이월) · A4-a 레퍼런스 해부 (반복)

둘은 09-06 판 그대로다 — [prompts-mobile-sprint-2026-09.md](prompts-mobile-sprint-2026-09.md) 의
**A5 · A4-a** 를 그대로 쓴다. A5 는 아직 미착수다(`spaceos-twin.web.app` 이 문서 20곳에 남아 있다).

---

# B군 — 클라우드 세션 지시 (claude.ai/code → PR)

> 각 프롬프트는 **대상 파일 · 입력 근거 · 통과 조건 · 금지** 넷을 채웠다.
> 하나라도 못 채우는 작업은 클라우드로 내보내지 않는다.

## B9 · 계정 화면 3종 ★KPI② 직결 · 가장 먼저

```
apps/frontend 에 계정 화면 3종을 만든다. 백엔드는 이미 있다 — 새로 만들지 말 것.

[확인부터] apps/backend/app/api/v1/auth.py 에 POST /signup · /login · /api-keys(POST·GET)가
이미 서 있다. 반면 apps/frontend/src/lib/api.ts 에는 auth 배선이 **0줄**이고
src/pages/ 에 계정 화면이 **없다**. 이 사실을 직접 확인하고 먼저 보고하라.

[왜] pppp_status.py 의 KPI② 블록이 계측 4/4 를 찍는데 파일럿은 0건이다.
사람이 가입해서 API 키를 받을 화면이 없다.

[대상 파일]
 신규: apps/frontend/src/pages/Signup.tsx · Login.tsx · ApiKeys.tsx (+ 각 .css)
 수정: apps/frontend/src/lib/api.ts (auth 함수 추가) · src/App.tsx (라우팅)

[할 일]
 1. apps/backend/app/api/v1/auth.py 와 app/schemas/ 의 실제 스키마를 먼저 읽고
    그 계약에 정확히 맞춘다. 필드를 추측하지 마라
 2. API 호출은 src/lib/api.ts 로 일원화한다(컴포넌트에서 직접 fetch 금지). 별칭은 @/
 3. 스타일은 src/styles/tokens.css 의 var() 와 @/design/components 의 Button·Card 를 쓴다.
    새 레이아웃 체계·새 색을 만들지 마라
 4. API 키는 **발급 직후 한 번만** 전문을 보이고 이후 마스킹. 폐기는 확인 단계를 둔다
 5. 토큰 보관 위치를 결정하고 그 이유를 PR 본문에 적는다.
    ⚠ API key 를 브라우저 저장소에 넣지 마라 — ProgramStudio 가 이미 그 규칙을 지킨다
 6. 라우팅은 기존 방식(#해시)을 따른다. 새 라우터 라이브러리를 깔지 마라

[통과 조건]
 - cd apps/frontend && npm run build 통과 (tsc -b 포함)
 - 세 화면이 기존 셸·토큰 위에 선다 — 스크린샷 대신 마크업 구조를 PR 본문에 적는다
 - 잘못된 비밀번호·중복 이메일 등 실패 응답의 화면 문구가 정의돼 있다
 - api.ts 밖에서 fetch 를 부르는 곳이 0곳임을 grep 으로 보인다

[금지]
 - 백엔드를 고치지 마라. 스키마가 불편해도 프론트를 맞춘다
 - 비밀번호를 로그·URL·쿼리스트링에 싣지 마라
 - 결제(payments.py)를 건드리지 마라. 이번 범위는 가입·로그인·키뿐이다
 - Tailwind 를 설치하지 마라
```

## B16 · M2-1 앵커 라벨 정렬 ★신뢰 · 화면보다 먼저

```
같은 앱에 공실률이 두 개 떠 있고 둘 다 라벨이 "공실률"인 것을 고친다.

[확인부터 — docs/finding-anchor-gap-2026-09.md §4-1 의 판정이다]
 heatmap 계열은 avg_vacancy 12.5% 를 쓰고(HubExplorer.tsx:300-317 · MapShell.tsx:405-409),
 예측 계열은 rone_aligned.mid 20.8% 를 쓴다(vacancy_forecast.py:37 · ml/inference/predictor.py:41).
 그리고 gold_vacancy.py:203-233 이 `anchor_gap_pp = avg_vacancy − anchor` 로 내려보낸다 —
 **정렬 안 된 값끼리 빼서 격차를 절반 이하로 축소한다**(연남 +7.5%p vs 정렬 격차 +15.8%p).
 이 세 자리를 직접 열어 사실을 먼저 보고하라.

[할 일 — 위 문서 §4-2 의 라벨 표를 그대로 구현한다. 라벨을 새로 발명하지 마라]
 주 지표  거점 전체 공실률 (실측·호실 기준)      12.5%
 대조 지표 중대형 상가 공실률 (R-ONE 정렬)        20.8%   ← rone_aligned.mid
 앵커     R-ONE 중대형 (최신 분기)               5.0%
 격차     정렬 격차 = 대조 지표 − 앵커           +15.8%p  ← 주 지표에서 빼지 않는다
 폭       불확실 구간                            15.2~27.6%  ← vacancy_floor_hi/lo_pct

[대상 파일]
 apps/backend/app/services/gold_vacancy.py (anchor_gap_pp 의 정의)
 apps/frontend/src/pages/HubExplorer.tsx · MapShell.tsx · PageDashboard.tsx

[통과 조건]
 - anchor_gap_pp 가 rone_aligned.mid 기준으로만 계산된다. 기존 필드를 조용히 바꾸지 말고
   이름을 분리한 뒤(예: aligned_gap_pp) 무엇이 무엇을 쓰는지 표로 보고하라
 - 화면에서 "공실률"이라는 같은 이름을 쓰는 서로 다른 두 수가 0곳임을 grep 으로 보인다
 - cd apps/backend && pytest 통과 · cd apps/frontend && npm run build 통과
 - 값이 바뀌는 API 응답 키를 전부 나열한다 (프론트가 안 깨지는지 같이 확인)

[금지]
 - 격차를 작아 보이게 만들지 마라. +15.8%p 가 실측이면 그대로 그린다
 - Gold 산출물을 다시 만들지 마라(클라우드에 bronze/silver 가 없다). 이미 있는
   calibration.json 의 rone_aligned 를 읽는다
 - 밴드(vacancy_floor_hi/lo)를 빼지 마라 — 대표값 단독은 소형에서 과소추정한다
 - ml/ 의 예측 경로를 건드리지 마라. 이번은 표시 계약이다
```

## B10 · D5-6 글자 정리 (재발행 — 대상이 커졌다)

```
화면당 글자 수를 줄인다. "결론 1줄 + 근거 3줄" 이 규칙이다.

[문제 — 2026-09-08 실측. 09-06 계획 때보다 늘었다]
 PlatformConsole.tsx 805 → 989줄 · ProgramStudio.tsx 611 → 772줄
 한 화면이 결론·근거·원자료를 동시에 편다.

[규칙]
 - 화면 맨 위에 그 화면의 결론 한 문장 — 스크롤 없이 보여야 한다
 - 그 아래 근거 3줄. 나머지는 접거나(<details>) 상세 패널로 민다. **지우지 마라**
 - 각 탭의 헤드라인은 그 탭이 답하는 질문이다
     Platform  이 입지·상권은 어떤 플랫폼인가
     Program   posting 한 page 를 어떤 홍보 program 으로 돌릴 것인가

[대상] apps/frontend/src/pages/PlatformConsole.tsx · ProgramStudio.tsx (둘만)

[통과 조건]
 - npm run build 통과
 - 화면별 텍스트 노드 수를 작업 전/후로 세어 보고 (목표 −50%)
 - 데이터를 화면에서 지우지 않았음을 확인 — 접힌 것과 지운 것은 다르다

[금지]
 - PageDashboard.tsx 는 건드리지 마라 (#board 해시로 밀려난 화면 · 이사 미완)
 - 수치를 반올림하거나 요약해 정확도를 낮추지 마라
 - 출처 표기("R-ONE 앵커" · "TRDAR 상권 단위" 등)를 접지 마라 — 그건 근거다
 - 지도 생명주기(MapHost)를 건드리지 마라
```

## B11 · D5-7 4P 플로우 — 디자이너 피드백 마지막 1개

```
탭 6개가 병렬로 놓여 순서를 말하지 않는 문제를 고친다.

[확인부터] grep -rn "다음 단계\|nextStep" apps/frontend/src 는 현재 **0건**이다.
사용자는 Platform → Page → Posting → Program 순서를 화면에서 알 수 없다.

[설계]
 1. 각 트랙 화면 하단에 "다음 단계" CTA 하나 — 어디로 가고 무엇을 얻는지 한 줄
 2. 순서: ① Platform → ② Page → ③ Posting → ④ Program
 3. 선택한 거점을 다음 화면이 이어받는다. 다시 고르게 하지 마라
 4. CTA 색은 --track-<다음 트랙> 을 쓴다(토큰은 이미 있다)

[대상 파일]
 apps/frontend/src/pages/PlatformConsole.tsx · PageDashboard.tsx
 apps/frontend/src/pages/PostingConsole.tsx · ProgramStudio.tsx
 (공통 CTA 를 만든다면 apps/frontend/src/design/components/ 아래 하나만)

[통과 조건]
 - npm run build 통과
 - design/planning/01-scenario-m1-yeonnam.md 의 ①~④ 를 **뒤로가기 없이** 탈 수 있음을
   각 화면의 CTA 목적지로 표에 적어 보인다
 - 기존 탭 직접 이동은 그대로 남는다 (강제 순서가 아니라 안내다)

[금지]
 - 탭을 없애거나 순서를 강제하지 마라. 지자체·프랜차이즈 페르소나는 건너뛴다
 - 라우팅 방식을 바꾸지 마라(#해시 유지)
 - 지도 생명주기(MapHost)를 건드리지 마라
```

## B12 · M2-2 "12개뿐?" 을 없앤다

```
Posting 화면이 공실 유닛 12개만 보여주고 그 이유를 말하지 않는 문제를 고친다.

[문제] data/gold/<hub>/vacant_units.json 은 거점당 12유닛인데
data/gold/<hub>/vacant_floor_units.json 은 296~559개다(66거점 합 12,497 —
confirmed 9,437 / probable 3,060). 화면이 12만 보이면 "연남동에 빈 자리가 12개뿐?"으로 읽힌다.

[답할 것 — 코드를 고치기 전에 먼저 보고하라]
 1. 12가 상한(선별)인가 전량인가. 어느 파이프라인이 12로 줄이는가 — 파일·줄로 대라
 2. 선별 기준이 무엇인가 (면적? 층? 용도?)

[할 일] 화면이 줄인 이유를 말하게 한다.
 예: "층별 공실 296곳 중 조건 적합 12곳" — 실제 기준으로 문구를 채운다
[대상 파일] apps/frontend/src/pages/PostingConsole.tsx (+ 필요하면 백엔드 응답에 총수 필드)

[통과 조건]
 - 숫자 둘(전체 층 단위 공실 · 화면에 뜬 유닛)이 같은 자리에 보이고 라벨이 서로 다르다
 - 두 수의 출처가 화면 또는 툴팁에 적힌다
 - npm run build 통과 · 백엔드를 고쳤다면 cd apps/backend && pytest 통과

[금지]
 - 숫자를 늘려 보이게 하려고 기준을 바꾸지 마라. 줄인 이유를 말하는 문제다
 - Gold 산출물을 다시 만들지 마라. 클라우드에는 bronze/silver 가 없다
```

## B13 · M2-3 커버리지 두 눈금 정리

```
서로 다른 것을 재는 커버리지 두 수가 섞여 있는 것을 정리한다.

[문제] data/gold/<hub>/coverage.json 의 coverage_pct 는 연남 41.7 · 신촌 39.0 · 홍대 60.1 인데
체인의 "정밀 커버리지"는 셋 다 100.0% 다. 전자는 노출/제외 비율, 후자는 분모 정밀도다.
하나만 쓰면 반드시 오해가 난다.

[답할 것 — 먼저 보고]
 1. 두 수가 각각 어디서 계산되는가 (파일·함수)
 2. 화면·자료에 어느 쪽을 낼 것인가. 판단 근거 한 줄
[할 일] 정한 쪽만 화면에 남기고, 둘 다 낸다면 라벨을 다르게 쓴다
[대상 파일] apps/frontend/src/pages/HubExplorer.tsx · AdminCoverage.tsx · 관련 백엔드 응답

[통과 조건]
 - 같은 화면에서 두 수가 같은 라벨로 나오는 곳이 0곳임을 grep 으로 보인다
 - npm run build 통과
 - ⚠ 함정을 문서에 남긴다: data/gold/*/coverage.json 을 세면 73 이 나오지만 그건
   서빙 거점 수가 아니다(서빙 66 + 경기 보류 산출물 7). 서빙 단일 기준은 districts.PAGES

[금지]
 - 커버리지가 낮은 거점을 화면에서 숨기지 마라(banpo 5.2% 는 경고색으로 이미 처리돼 있다)
 - 산출물 재계산을 시도하지 마라
```

## B14 · D5-2 잔여 — 하드코딩 색 388 → 100 이하

```
B3(09-06)이 절반만 닫았다. 남은 하드코딩 색을 토큰으로 마저 옮긴다.

[실측 2026-09-08]
 grep -oE "#[0-9a-fA-F]{3,8}" src/pages/*.css src/components/*.css src/App.css | wc -l  → 388
 grep -o "var(--" ... | wc -l → 543   (채택률 58.3% · 목표 80%)
 D5 통과 조건은 "hex 100 이하 · var 채택률 ≥80%" 였다.

[할 일]
 1. 388개를 파일별로 세어 목록을 먼저 낸다
 2. tokens.css 에 대응 변수가 **이미 있는** 색만 var() 로 바꾼다
 3. 대응이 없는 색은 그대로 두고, 몇 개가 왜 남았는지 표로 보고한다.
    새 토큰이 필요하다고 판단되면 제안만 하고 만들지 마라
 4. --track-* 4색과 충돌하는 자리가 있으면 표시만 한다

[통과 조건]
 - npm run build 통과
 - 위 두 grep 수를 작업 전/후로 보고. hex 100 이하 · var 채택률 80% 이상
 - 레이아웃·색 변화 0. 픽셀이 움직였다면 실패다(같은 값의 이름만 바뀐다)

[금지]
 - .tsx 인라인 스타일과 지도 오버레이 색(MapShell.tsx 의 fillColor 등)은 건드리지 마라.
   SDK 가 인라인으로 그리는 자리라 CSS 변수가 닿지 않는다
 - 새 색을 만들지 마라 · Tailwind 를 설치하지 마라
```

## B15 · 논문 — Page 문헌 방법 근거표

```
docs/papers/workflow-validation.md 가 지목한 "바로 다음 산출물"을 만든다.

[그 문장] "바로 다음 산출물은 Page 핵심 문헌의 원문·부록 목록과 페이지별 방법 근거표가 적절하다."

[먼저 읽을 것]
 docs/papers/workflow-validation.md (권장 실행 순서 1~2)
 docs/papers/page-study/source-register.json · reading-ledger.md · design-and-variables.md

[만들 것] docs/papers/page-study/method-evidence-table.md
 문헌마다 행: 연구 질문 / 이론 / 가설·검증 속성 / 관측 단위 / 타깃 / 표집·분할 /
 기준선 / 수식·통계 / 한계 — 각 칸에 **원문 위치**(페이지·절)를 붙인다
 기존 후보표의 Zhang·Gosline 실험 설명은 정정 대상으로 명시돼 있다 — 원문으로 대조하라

[통과 조건]
 - 모든 칸이 원문 위치 또는 "미확보"를 말한다. 빈칸을 두지 마라
 - source-register.json 에 권리·버전·해시·접근 상태를 갱신한다
 - 원문을 못 구한 문헌은 "링크 + 접근 필요"로 남긴다. 요약으로 대체하지 마라

[금지]
 - 원문을 안 보고 초록·요약으로 칸을 채우지 마라. 그게 이 문서가 경고한 실패다
 - 참고문헌의 문장을 복제하지 마라. 출처를 인용하고 적용 변경을 설명한다
 - PlaceOS 실측 수치를 새로 만들지 마라 — 근거 인덱스에 등재된 것만 쓴다
```

---

## 2. 폰으로는 못 하는 것 (데스크톱 대기)

| 작업 | 왜 폰에서 안 되나 |
|---|---|
| **C1 후속** — 페이지 리셋 A/B 로 누적 가설 검정 (`finding-map-hubswitch-2026-09-08.md` §7-4) | 네이버 지도 SDK 키 + 로컬 서버 + playwright 실행이 필요하다. **원인 확정 전에 코드를 고치지 말라고 그 문서가 못박았다** |
| **M4** 3거점 C1~C6 검증 | 지도 픽셀을 봐야 한다 |
| **M5b** 66거점 화면 루프 실행(264조합) | 같음 · 야간 무인 실행(`/autorun`) |
| 수집(건축HUB·R-ONE·네이버) · GNN/LSTM 재학습 | `.gitignore` 가 bronze/silver 를 막아 클라우드에 원천이 없다 |
| Lovable 산출 코드를 `apps/landing/` 으로 편입 | 클론·빌드·배포 라인 작업 |

---

## 부록. 클라우드 세션에 걸기 전 (데스크톱에서 1분)

```bash
git status --short          # 비어 있어야 한다
git push origin main        # ⚠ 반드시 백그라운드로 (GCM 인증 창)
```

수치를 다시 재야 하면:

```bash
python scripts/pppp_status.py | grep -v '└'
python scripts/chain_status.py --all
cd apps/frontend && grep -oE "#[0-9a-fA-F]{3,8}" src/pages/*.css src/components/*.css src/App.css | wc -l
cd apps/frontend && wc -l src/pages/*.tsx | sort -rn | head -5
```
